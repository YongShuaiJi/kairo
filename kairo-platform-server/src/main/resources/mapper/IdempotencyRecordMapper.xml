<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.kairo.platform.persistence.mapper.IdempotencyRecordMapper">

    <select id="activeRecord" resultType="map">
        select *
          from idempotency_record
         where idempotency_key = #{idempotencyKey}
           and expires_at > current_timestamp
         limit 1
    </select>

    <insert id="insertRecord">
        insert into idempotency_record(
            idempotency_key, actor, request_hash, response_status, response_json, created_at, expires_at
        ) values (
            #{idempotencyKey}, #{actor}, #{requestHash}, #{responseStatus},
            #{responseJson}, #{createdAt}, #{expiresAt}
        )
    </insert>
</mapper>
