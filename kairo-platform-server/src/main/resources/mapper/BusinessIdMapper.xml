<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.kairo.platform.persistence.mapper.BusinessIdMapper">

    <update id="incrementSequence">
        update fencing_sequence
           set current_value = current_value + 1,
               updated_at = #{updatedAt}
         where resource_key = #{resourceKey}
    </update>

    <insert id="insertSequence">
        insert into fencing_sequence(resource_key, current_value, updated_at)
        values (#{resourceKey}, #{currentValue}, #{updatedAt})
    </insert>

    <select id="currentSequence" resultType="long">
        select current_value
          from fencing_sequence
         where resource_key = #{resourceKey}
    </select>

    <update id="updateSequenceValue">
        update fencing_sequence
           set current_value = #{currentValue},
               updated_at = #{updatedAt}
         where resource_key = #{resourceKey}
    </update>

    <update id="updateSequenceAtLeast">
        update fencing_sequence
           set current_value = greatest(current_value, #{currentValue}),
               updated_at = #{updatedAt}
         where resource_key = #{resourceKey}
    </update>
</mapper>
