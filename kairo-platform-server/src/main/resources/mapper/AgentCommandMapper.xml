<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.kairo.platform.persistence.mapper.AgentCommandMapper">

    <select id="listCommands" resultType="map">
        select *
          from agent_command
         order by created_at, id
    </select>

    <insert id="insertCommand">
        insert into agent_command(
            id, agent_id, command_type, status, idempotency_key, payload_json, result_json,
            attempts, max_attempts, available_at, created_by, created_at, updated_at, correlation_id,
            rollback_execution_id
        ) values (
            #{id}, #{agentId}, #{commandType}, #{status}, #{idempotencyKey}, #{payloadJson}, #{resultJson},
            #{attempts}, #{maxAttempts}, #{availableAt}, #{createdBy}, #{createdAt}, #{updatedAt}, #{correlationId},
            #{rollbackExecutionId}
        )
    </insert>

    <select id="pollCandidates" resultType="map">
        select *
          from agent_command
         where agent_id = #{agentId}
           and (
                (status = 'PENDING' and available_at &lt;= #{now})
             or (status = 'DISPATCHED' and lease_expires_at &lt;= #{now} and attempts &lt; max_attempts)
           )
         order by available_at, created_at, id
         limit 1
    </select>

    <update id="dispatchCommand">
        update agent_command
           set status = 'DISPATCHED',
               attempts = attempts + 1,
               dispatched_at = #{dispatchedAt},
               lease_expires_at = #{leaseExpiresAt},
               updated_at = #{updatedAt}
         where id = #{id}
           and (
                status = 'PENDING'
             or (status = 'DISPATCHED' and lease_expires_at &lt;= #{dispatchedAt})
           )
    </update>

    <update id="ackCommand">
        update agent_command
           set status = #{status},
               result_json = #{resultJson},
               error_message = #{errorMessage},
               completed_at = #{completedAt},
               updated_at = #{updatedAt}
         where id = #{id}
           and status = 'DISPATCHED'
        <choose>
            <when test="expectedAttempts != null">
                and attempts = #{expectedAttempts}
            </when>
            <otherwise>
                and attempts = 1
            </otherwise>
        </choose>
    </update>

    <!--
      V1.7 M1-A sect 8.1: terminate a dispatched command whose lease expired and that exhausted
      max_attempts, so it can never linger invisibly in DISPATCHED. The fixed error code is
      stored in error_message (no dedicated error_code column exists; migrations are frozen).
      Agent-scoped to the polling agent: the M1-A trigger (restart recovery is M1-B).
    -->
    <update id="expireExhaustedCommands">
        update agent_command
           set status = 'FAILED',
               error_message = 'AGENT_COMMAND_MAX_ATTEMPTS_EXHAUSTED',
               completed_at = #{now},
               updated_at = #{now}
         where agent_id = #{agentId}
           and status = 'DISPATCHED'
           and lease_expires_at &lt;= #{now}
           and attempts &gt;= max_attempts
    </update>

    <!--
      V1.7 M1-B sect 8.2: restart recovery input. Non-terminal commands (PENDING or
      DISPATCHED) created before this process's startup boundary, projected to only the
      columns the recovery needs. payload_json and result_json are intentionally excluded
      so the recovery never loads sensitive
      transient material (script source, class bytes, tokens). The DURABLE/TRANSIENT split
      is applied in Java (AgentCommandClassification); DURABLE rows returned here are
      left untouched so they stay claimable/redispatchable under M1-A.
    -->
    <select id="findPendingOrDispatchedCommands" resultType="map">
        select id, command_type, status, attempts
          from agent_command
         where status in ('PENDING', 'DISPATCHED')
           and created_at &lt; #{createdBefore}
    </select>

    <!--
      V1.7 M1-B sect 8.2: atomically fail one orphan TRANSIENT command with the fixed
      TRANSIENT_COMMAND_CONTEXT_LOST code (passed in so the constant is owned by
      AgentCommandClassification). Guarded on status IN (PENDING, DISPATCHED) so the
      recovery is idempotent per row: a second startup matches zero rows for an
      already-terminal command and records no duplicate audit. No dedicated error_code
      column exists (migrations are frozen), so the code is stored in error_message,
      mirroring M1-A's AGENT_COMMAND_MAX_ATTEMPTS_EXHAUSTED convention.
    -->
    <update id="failTransientCommand">
        update agent_command
           set status = 'FAILED',
               error_message = #{errorCode},
               completed_at = #{now},
               updated_at = #{now}
         where id = #{id}
           and status in ('PENDING', 'DISPATCHED')
    </update>

    <update id="updateAgentStatus">
        update agent_instance
           set status = #{status},
               updated_at = #{updatedAt}
         where id = #{id}
    </update>

    <select id="rollbackExecution" resultType="map">
        select *
          from rollback_execution
         where id = #{id}
    </select>

    <update id="completeRollbackExecution">
        update rollback_execution
           set status = #{status},
               finished_at = #{finishedAt}
         where id = #{id}
           and status = 'DISPATCHED'
    </update>

    <update id="updateUnloadingOperationStatus">
        update operation_plan
           set status = #{status},
               version = version + 1,
               updated_by = #{updatedBy},
               updated_at = #{updatedAt}
         where id = #{id}
           and status = 'UNLOADING'
    </update>

    <select id="operationPlan" resultType="map">
        select *
          from operation_plan
         where id = #{id}
    </select>

    <select id="executionsByCommand" resultType="map">
        select *
          from rollout_instance_execution
         where command_id = #{commandId}
    </select>

    <update id="completeExecution">
        update rollout_instance_execution
           set status = #{status},
               error_message = #{errorMessage},
               finished_at = #{finishedAt},
               version = version + 1,
               updated_by = #{updatedBy},
               updated_at = #{updatedAt}
         where id = #{id}
           and status = 'WAITING_AGENT'
           and version = #{version}
    </update>

    <select id="rolloutExecution" resultType="map">
        select *
          from rollout_instance_execution
         where id = #{id}
    </select>

    <select id="executionsByOperation" resultType="map">
        select *
          from rollout_instance_execution
         where operation_plan_id = #{operationPlanId}
    </select>

    <update id="completeRunningOperation">
        update operation_plan
           set status = #{status},
               version = #{version},
               updated_by = #{updatedBy},
               updated_at = #{updatedAt}
         where id = #{id}
           and status = 'RUNNING'
           and version = #{currentVersion}
    </update>

    <select id="successfulExecutions" resultType="map">
        select instance_id
          from rollout_instance_execution
         where operation_plan_id = #{operationPlanId}
           and status = 'SUCCEEDED'
    </select>

    <update id="updateRuleRuntimeStatusActive">
        update rule_runtime_status
           set status = 'ACTIVE',
               last_error = null,
               updated_at = #{updatedAt}
         where rule_id = #{ruleId}
           and rule_version = #{ruleVersion}
           and instance_id = #{instanceId}
    </update>

    <!--
      V1.7 M1-E sect 8.5: per-instance REMOVED marking. A multi-target unload records each
      instance's real outcome; REMOVED is set only for the instance whose RESET_CLASS actually
      unloaded the rule (or whose enhancement was confirmed gone on a new empty JVM), never in
      bulk on aggregate completion.
    -->
    <update id="updateRuleRuntimeStatusRemoved">
        update rule_runtime_status
           set status = 'REMOVED',
               last_error = null,
               updated_at = #{updatedAt}
         where rule_id = #{ruleId}
           and rule_version = #{ruleVersion}
           and instance_id = #{instanceId}
    </update>

    <insert id="insertRuleRuntimeStatus">
        insert into rule_runtime_status(
            id, rule_id, rule_version, instance_id, status,
            hit_count, error_count, last_error, updated_at
        ) values (
            #{id}, #{ruleId}, #{ruleVersion}, #{instanceId}, 'ACTIVE',
            0, 0, null, #{updatedAt}
        )
    </insert>

    <update id="transitionOperationToUnloading">
        update operation_plan
           set status = 'UNLOADING',
               version = version + 1,
               terminal_source = 'ROLLOUT_FAILURE',
               terminal_reason = '实例执行失败后自动卸载',
               updated_by = #{updatedBy},
               updated_at = #{updatedAt}
         where id = #{id}
           and status = 'RUNNING'
           and version = #{version}
    </update>

    <insert id="insertRollbackExecution">
        insert into rollback_execution(
            id, operation_plan_id, rollback_type, status, reason,
            target_class_id, target_class_name, created_by, created_at, finished_at
        ) values (
            #{id}, #{operationPlanId}, 'RESET_CLASS', 'DISPATCHED', #{reason},
            #{targetClassId}, #{targetClassName}, #{createdBy}, #{createdAt}, null
        )
    </insert>

    <!--
      V1.7 M1-E sect 8.5: the previous activeAgentsForOperation / markRollbackSucceeded /
      markUnloadingOperationUnloadedWithoutAgents statements are intentionally removed. The no-agent
      short-cut fabricated operation_plan UNLOADED the moment no agent was reachable; an unload that
      cannot reach its agents now stays UNLOADING and completes from the actual snapshot on
      reconnect (M1-E compensation), and each instance records its own outcome via updateExecutionStatus.
    -->

    <!--
      V1.7 M1-E sect 8.5: per-instance unload state, keyed by (operation, instance). Guarded on a
      non-terminal current status so a terminal instance is never clobbered. UNLOADING = RESET_CLASS
      dispatched; OFFLINE_PENDING = agent offline (compensation pending); UNLOADED = acked /
      confirmed gone; FAILED = exhausted.
    -->
    <update id="updateExecutionStatus">
        update rollout_instance_execution
           set status = #{status},
               error_message = #{errorMessage},
               finished_at = coalesce(finished_at, #{finishedAt}),
               version = version + 1,
               updated_by = #{updatedBy},
               updated_at = #{updatedAt}
         where operation_plan_id = #{operationPlanId}
           and instance_id = #{instanceId}
           and status not in ('UNLOADED', 'FAILED', 'CANCELLED', 'ABANDONED')
    </update>

    <select id="operationResource" resultType="map">
        select resource_type, resource_id
          from operation_plan
         where id = #{id}
    </select>

    <select id="countAgent" resultType="int">
        select count(*)
          from agent_instance
         where id = #{id}
    </select>

    <select id="findAgentCapabilities" resultType="string">
        select capabilities_json
          from agent_instance
         where id = #{agentId}
    </select>

    <select id="lockAgentForRuntimeStateRefresh" resultType="map">
        select id
          from agent_instance
         where id = #{agentId}
         for update
    </select>

    <select id="findNonTerminalCommandsByAgentAndType" resultType="map">
        select id, status, idempotency_key
          from agent_command
         where agent_id = #{agentId}
           and command_type = #{commandType}
           and status in ('PENDING', 'DISPATCHED')
         order by created_at, id
    </select>

    <select id="commandById" resultType="map">
        select *
          from agent_command
         where id = #{id}
    </select>

    <select id="commandByIdempotencyKey" resultType="map">
        select *
          from agent_command
         where idempotency_key = #{idempotencyKey}
    </select>
</mapper>
