<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.kairo.platform.persistence.mapper.FencingTokenMapper">

    <insert id="insertToken">
        insert into fencing_token(
            id, resource_type, resource_id, purpose, token, sequence, owner, status,
            lease_expires_at, created_at, correlation_id
        ) values (
            #{id}, #{resourceType}, #{resourceId}, #{purpose}, #{token}, #{sequence}, #{owner}, #{status},
            #{leaseExpiresAt}, #{createdAt}, #{correlationId}
        )
    </insert>

    <update id="consumeIssuedToken">
        update fencing_token
           set status = 'CONSUMED',
               consumed_at = #{now}
         where resource_type = #{resourceType}
           and resource_id = #{resourceId}
           and token = #{token}
           and owner = #{owner}
           and status = 'ISSUED'
           and lease_expires_at &gt; #{now}
    </update>
</mapper>
