# Runtime Mock 核心功能缺口

本文档记录当前联调和真实使用过程中发现、需要集中补齐的核心功能。这里记录的事项不是普通体验优化，而是影响产品核心闭环的问题。

## 完成与验收记录

### 总体状态

已完成（2026-06-20）。

本文档记录的五项核心缺口已经完成实现、自动化回归和真实浏览器端到端验收。验收使用真实启动的 `runtime-mock-demo`、真实 Java Agent、Platform Server、PostgreSQL、Redis、Kafka、MinIO 和 Platform Web，不使用前端演示数据或手工伪造实例。

### 真实端到端验收结果

1. 清空数据库中的旧实例和测试数据后，以 `-javaagent` 启动 `runtime-mock-demo`。
2. Agent 自动采集真实 PID、主机名、JDK 版本、JVM 启动信息、Agent 版本与能力并完成注册。
3. 未指定环境时，实例以“待分配环境”状态进入 Platform；随后在 Web 中从固定环境列表选择“开发环境（env-dev）”完成分配。
4. 在创建规则页面选择真实应用和环境，通过 Agent 主动命令发现并选择：
   - 类：`com.example.demo.OrderService`
   - 方法：`calculateScore(int)`
   - JVM 描述符：`(I)I`
5. 创建规则“本地字节码效果验证”，脚本为 `return mock.returnValue(999)`，服务端校验和草稿保存成功。
6. 创建发布计划时，应用、环境、规则和规则版本均从真实资源中级联选择；规则展示为“名称（ID）”。
7. 同一用户完成“提交审批 → 批准发布 → 调度发布 → 开始执行”，自审批记录正常生成。
8. 灰度批次、实例执行和 Agent 命令均进入“已成功”状态。
9. 调用真实测试应用接口验证字节码效果：

```text
发布前：GET /demo/score?base=21 -> {"score":42,"base":21}
发布后：GET /demo/score?base=21 -> {"score":999,"base":21}
Reset 后：GET /demo/score?base=21 -> {"score":42,"base":21}
```

10. Web 中规则、发布、审批、批次、执行、命令、实例等已知状态和业务类型统一显示中文；英文机器代码仍保留在 API、数据库和技术详情中。

### 本次验收资源

- 实例：`instance-4df6634b-2031-4521-a6b5-5649dcd33b68`
- Agent：`agent-248e0f26-2600-4b61-bd28-52de1e5aea94`
- 规则：`rule-3bae5151-16c6-4d59-bf7a-339b25e7a3e2`
- 发布计划：`operation-db14792f-4eb0-4d4f-b460-4ae4ad7d41d4`
- 审批单：`approval-ff73e7be-8195-4466-8acb-a4e073648e30`
- 灰度批次：`rollout-batch-2ec2840d-3b9d-4927-bf93-1c1583232c31`
- 实例执行：`rollout-execution-054ffa7b-e4cf-4e5b-a309-9b3176559f51`
- Agent 命令：`agent-command-a401511e-706e-47f6-ba5d-32adece3a7bd`

这些 ID 是一次真实验收运行产生的审计证据，不是固定种子数据；重新清库并运行时会生成新的 ID。

### 自动化回归

- 全仓库 Maven 单元测试与集成测试通过：`mvn test`
- Platform 目标发现、自注册、环境分配、级联约束、自审批和发布执行集成测试通过
- Platform Web ESLint、TypeScript 和生产构建通过
- Docker Compose 中 Platform 与 Platform Web 健康检查通过

## 1. 中央平台无法自动发现在线 JVM 的目标类与方法

### 状态

已完成（2026-06-20）。

### 问题表现

在 Platform Web 创建规则时，用户选择应用和环境后，“目标方法”区域无法显示当前在线 Agent 所在 JVM 已加载的类和方法，并提示：

> 没有找到已登记的方法。

用户目前只能手动填写类名和方法名，无法通过中央平台可靠地浏览和选择真实运行时目标。

### 已验证事实

- 本地测试应用 `runtime-mock-demo` 已通过 `-javaagent` 启动。
- Agent 能够实时读取 JVM 已加载类。
- Agent 已识别 `com.example.demo.OrderService`。
- 该类状态为 `modifiable: true`。
- Agent 能列出该类的完整方法、参数类型、返回类型和 JVM 方法描述符。
- Platform Web 调用的 `GET /api/v1/targets/search` 返回空数组。

### 根因

当前 `GET /api/v1/targets/search` 只查询 PostgreSQL 中已有规则版本的 `rule_target` 表。

因此，该接口实际上只能搜索“历史上已配置过规则的方法”，并不能搜索当前在线 JVM 中真实存在的方法。数据库重置或尚未创建规则时，即使 Agent 在线且已经识别目标类，中央平台仍然无法发现目标。

当前页面文案、接口名称和真实能力不一致。

### 正确产品行为

1. 用户选择应用和环境。
2. Platform 找到该范围内处于在线状态的实例和 Agent。
3. Platform 通过现有 Agent 主动轮询命令通道发起运行时目标发现。
4. Agent 从 `Instrumentation.getAllLoadedClasses()` 获取当前 JVM 已加载类。
5. Agent 根据查询条件筛选类，并读取可用方法及其完整签名。
6. Platform 汇总多个 Agent 的发现结果并返回给 Web。
7. Web 展示可搜索、可选择的真实目标类和方法。
8. 手动填写仅作为 Agent 离线、发现失败或特殊目标场景下的降级能力。

### 架构约束

- Platform 不应直接访问 Agent 的 `127.0.0.1` 监听地址。
- 需要兼容 Agent 位于 NAT、容器、内网或无法被 Platform 反向访问的场景。
- 应复用现有 Agent 主动轮询命令机制，不新增反向网络依赖。
- 发现操作必须受 Agent Token、用户权限和应用/环境范围约束。
- 不应把 JVM 的全部类无筛选、无上限地长期写入 PostgreSQL。

### 建议实现

#### Platform

- 新增 `DISCOVER_TARGETS` Agent 命令。
- 命令参数至少包括：
  - `query`
  - `limit`
  - `includeMethods`
  - 可选包名前缀
- 为发现命令增加短时结果存储或 Redis 缓存。
- `GET /api/v1/targets/search` 优先返回在线 Agent 的实时或短时缓存结果。
- 历史 `rule_target` 只作为补充结果，不再作为唯一数据源。
- 合并多个实例结果时标记实例覆盖情况和签名差异。

#### Agent

- 执行 `DISCOVER_TARGETS` 命令。
- 返回字段至少包括：
  - `classId`
  - `className`
  - `classLoaderId`
  - `classLoaderClassName`
  - `modifiable`
  - `methodName`
  - `methodDescriptor`
  - `returnType`
  - `parameterTypes`
  - `modifiers`
  - `static`
  - `private`
- 对查询数量、执行耗时和响应大小设置安全上限。

#### Platform Web

- 选择应用和环境后自动加载推荐目标。
- 输入类名或方法名时进行防抖搜索。
- 展示类名、方法名、参数、返回值和方法描述符。
- 展示目标出现在哪些实例，以及是否所有实例签名一致。
- 区分以下状态：
  - 尚未选择应用和环境
  - 正在发现
  - 未发现匹配目标
  - Agent 离线
  - 部分实例发现失败
  - 发现成功
- 将“没有找到已登记的方法”修改为符合真实状态的提示。

### 验收标准

- 启动 `runtime-mock-demo` 和 Agent 后，无需预先创建任何规则。
- 在 Platform Web 选择 `app-default` 和 `env-dev`。
- 搜索 `OrderService` 能看到 `com.example.demo.OrderService`。
- 能选择 `calculateScore(int)`，并获得描述符 `(I)I`。
- 创建并发布返回 `999` 的规则后：

```bash
curl 'http://127.0.0.1:18090/demo/score?base=21'
```

返回的 `score` 从原始值 `42` 变为 `999`。

- 禁用、删除规则或执行 Reset 后，返回值恢复为 `42`。
- 多实例方法签名不一致时，前端必须明确提示，不得静默选择。
- Agent 离线或发现超时时，用户仍可明确切换到手动填写模式。
- 自动化测试覆盖目标发现、权限、超时、结果合并和降级流程。

## 2. 前后端状态、操作与业务字段缺少统一中文语义

### 状态

已完成（2026-06-20）。

### 问题表现

规则、发布计划、审批、Agent 命令和实例执行等页面直接展示后端英文状态枚举，例如：

- `DRAFT`
- `WAITING_APPROVAL`
- `APPROVED`
- `RUNNING`
- `EXPIRED`
- `CANCELLED`

后端业务异常也直接将英文信息返回给用户，例如：

> An approved decision bound to the current resource version is required

详情抽屉和其他资源页面还直接暴露数据库/API 字段名、资源类型、发布类型、策略代码以及状态迁移目标，例如：

- `application_id`
- `environment_id`
- `plan_type`
- `resource_type`
- `resource_id`
- `resource_version`
- `strategy_json`
- `approval_id`
- `RULE_ROLLOUT`
- `rule`
- `canary`

页面底部操作按钮也直接显示机器状态：

- `APPROVED`
- `CANCELLED`
- `EXPIRED`

这使中文控制台在关键操作反馈上出现语言割裂，用户难以理解字段含义、状态含义、操作结果、失败原因及下一步操作，整个详情页也更像数据库调试工具而不是正式产品。

### 正确产品行为

1. Web 页面、抽屉、表格、筛选器、按钮、提示消息和错误反馈统一展示中文业务语义。
2. 状态、状态迁移按钮、资源类型、计划类型、字段名称和常见策略值均使用中文展示。
3. 后端为状态、业务字段、资源类型和业务错误提供稳定、统一的中文语义，不再依赖前端临时翻译英文句子。
4. 页面不得直接把数据库行或 API 原始对象逐字段渲染给普通用户。
5. 机器字段与内部 ID 可以在“技术详情”中查看和复制，但不能作为主要业务展示。
6. 数据库存储、领域枚举和接口机器值继续使用稳定的英文代码，避免破坏状态机、历史数据和 API 兼容性。
7. API 返回机器可识别的 `code`，并提供中文 `message`；必要时同时返回 `statusLabel`、`typeLabel`、`fieldLabels`、`details` 和可执行的下一步建议。
8. 前端未知状态或类型必须安全降级，显示后端标签或原始代码，不得显示空白。

### 建议状态映射

| 状态代码 | 中文显示 |
| --- | --- |
| `DRAFT` | 草稿 |
| `WAITING_APPROVAL` | 待审批 |
| `APPROVED` | 已批准 |
| `REJECTED` | 已拒绝 |
| `SCHEDULED` | 已计划 |
| `PENDING` | 待处理 |
| `QUEUED` | 排队中 |
| `RUNNING` | 运行中 |
| `OBSERVING` | 观察中 |
| `SUCCEEDED` | 已成功 |
| `PARTIAL` | 部分成功 |
| `FAILED` | 已失败 |
| `CANCELLED` | 已取消 |
| `EXPIRED` | 已过期 |
| `ROLLING_BACK` | 回滚中 |
| `ROLLED_BACK` | 已回滚 |
| `ACTIVE` | 活跃 |
| `INACTIVE` | 未启用 |
| `OFFLINE` | 离线 |

最终实现时应扫描全部领域枚举并补齐映射，不能只处理上述截图中出现的状态。

### 建议操作按钮映射

| 机器状态/动作 | 中文按钮 |
| --- | --- |
| `WAITING_APPROVAL` | 提交审批 |
| `APPROVED` | 批准 |
| `REJECTED` | 拒绝 |
| `RUNNING` | 开始发布 |
| `CANCELLED` | 取消计划 |
| `EXPIRED` | 标记过期 |
| `ROLLING_BACK` | 开始回滚 |
| `PAUSED` | 暂停 |
| `RESUMED` | 恢复 |

按钮必须使用“动词 + 业务对象”表达用户将执行的操作，不能直接拿目标状态枚举作为按钮文案。危险操作应使用确认对话框说明影响范围。

### 建议字段名称映射

| API/数据库字段 | 中文显示 |
| --- | --- |
| `id` | 发布计划 ID / 当前资源 ID |
| `application_id` | 应用 |
| `environment_id` | 环境 |
| `plan_type` | 发布类型 |
| `resource_type` | 资源类型 |
| `resource_id` | 规则 / 目标资源 |
| `resource_version` | 资源版本 |
| `status` | 当前状态 |
| `version` | 计划版本 |
| `strategy_json` | 发布策略 |
| `approval_id` | 审批单 |
| `created_by` | 创建人 |
| `created_at` | 创建时间 |
| `updated_by` | 更新人 |
| `updated_at` | 更新时间 |

同一个字段在不同资源中应根据业务上下文使用更准确的名称，例如发布计划中的 `resource_id` 应显示为“规则”，而不是笼统显示“资源 ID”。

### 建议业务类型映射

| 机器代码 | 中文显示 |
| --- | --- |
| `RULE_ROLLOUT` | 规则发布 |
| `RULE_ROLLBACK` | 规则回滚 |
| `rule` / `RULE` | 规则 |
| `RECORDING_RULE` | 录制规则 |
| `DATASET_VERSION` | 数据集版本 |
| `REPLAY_PLAN` | 回放计划 |
| `canary` / `CANARY` | 灰度发布 |
| `SEQUENTIAL` | 顺序发布 |
| `PARALLEL` | 并行发布 |

策略 JSON 不应原样作为主要信息展示。前端应将已知结构转化为中文摘要，例如：

```text
发布方式：灰度发布
观察时间：60 秒
批次方式：顺序执行
每批实例：1
自动回滚：已开启
```

原始 JSON 仅放在可折叠的“技术详情”区域。

### 后端要求

- 建立统一的状态描述和错误消息目录。
- 建立统一的业务类型、字段元数据和操作语义目录，避免不同接口对同一概念使用不一致的中文。
- `PlatformException` 等业务异常返回稳定错误码和中文消息。
- 示例：`APPROVAL_REQUIRED` 的用户消息改为“当前资源版本尚未获得有效审批，请先发起并完成审批”。
- 错误详情应包含审批主体类型、主体 ID、版本等可定位信息，但不得暴露 Token、密钥或敏感脚本。
- OpenAPI 文档补充状态代码、业务类型、字段中文语义和允许的状态迁移。
- 查询与详情接口可返回关联资源的名称及显示标签，避免前端只能展示裸 ID。
- 状态迁移接口仍接收英文目标状态，但返回中文操作结果和当前状态标签。
- 日志和审计记录保留英文机器代码，必要时额外记录中文展示文本，不以中文文本替代可检索代码。

### Platform Web 要求

- 建立集中式状态、字段、类型、操作和策略映射组件或字典，禁止各页面散落硬编码。
- 表格、详情、徽标、筛选项、统计卡片和状态迁移按钮复用同一套语义映射。
- 状态颜色与语义保持一致：成功、处理中、警告、失败、禁用等不得在不同页面出现冲突。
- 详情抽屉使用按资源设计的中文字段布局，不得通过 `Object.entries()` 等方式直接渲染后端原始字段。
- 应用、环境、规则和审批单展示“名称（ID）”，而不是只显示裸内部 ID。
- 状态迁移按钮显示“批准”“取消计划”“标记过期”等中文动作，并根据当前状态和权限决定是否可用。
- 已知 JSON 策略显示为中文结构化摘要；原始 JSON 放入可折叠技术详情。
- 统一翻译后端业务错误；优先展示后端中文 `message`，并根据错误码补充下一步操作。
- `APPROVAL_REQUIRED` 应明确引导用户进入审批中心，而不是只显示错误 Toast。

### 验收标准

- 规则列表中的 `DRAFT` 显示为“草稿”。
- 发布计划中的 `WAITING_APPROVAL`、`EXPIRED`、`CANCELLED` 分别显示为“待审批”“已过期”“已取消”。
- 审批、发布、实例执行、Agent 命令等所有页面不再直接暴露已知英文状态枚举。
- 发布计划详情中的字段显示为“应用”“环境”“发布类型”“规则”“资源版本”“当前状态”“发布策略”“审批单”等中文业务名称。
- `RULE_ROLLOUT` 显示为“规则发布”，`rule` 显示为“规则”，`canary` 显示为“灰度发布”。
- `APPROVED`、`CANCELLED`、`EXPIRED` 操作按钮分别显示为“批准”“取消计划”“标记过期”。
- 所有类似资源详情页面完成扫描，不再直接暴露数据库 snake_case 字段名、英文类型代码或机器状态按钮。
- 发布策略默认显示中文摘要，原始 JSON 仅在技术详情中查看。
- 缺少审批时显示中文原因，并提供进入审批流程的明确操作入口。
- API 仍返回稳定英文状态代码，现有状态机和接口调用不受影响。
- 前后端自动化测试覆盖状态、字段、类型、操作和策略映射，未知代码降级以及主要业务错误的中文响应。

## 3. 实例与 Agent 缺少真实运行时自注册和身份管理

### 状态

已完成（2026-06-20）。

### 问题表现

“应用与环境”页面展示的实例虽然是真实数据库记录，但关键身份和运行时信息来自本地联调时的手工注册或固定默认值，并非运行中 JVM 和 Agent 自动采集、注册与持续更新的真实数据。

当前本地实例示例：

| 字段 | 当前值 | 实际来源 |
| --- | --- | --- |
| `id` | `runtime-mock-demo-local` | 联调时手工指定 |
| `application_id` | `app-default` | 数据库迁移预置默认应用 |
| `environment_id` | `env-dev` | 数据库迁移预置默认环境 |
| `hostname` | `localhost` | 联调时手工指定 |
| `process_id` | `pending` | 占位符，不是真实 PID |
| `runtime` | `java-21` | 联调时手工指定 |
| `labels_json` | `{"purpose":"bytecode-test","tier":"local-demo"}` | 联调时手工填写 |
| Agent ID | `runtime-mock-demo-agent` | 启动参数 `platformAgentId` 手工关联 |

这会让真实环境中的实例看起来像演示数据，并带来更严重的问题：

- Platform 无法确认记录是否对应当前正在运行的 JVM。
- JVM 重启后不能可靠地区分旧进程和新进程。
- `ACTIVE` 状态可能只是创建时写入，不能证明实例仍然在线。
- 发布计划可能向错误实例、过期实例或仅存在于数据库中的实例下发。
- 真实 PID、主机、JDK、启动时间、Agent 版本和运行能力不可验证。
- 应用和环境归属依赖手工输入，容易错绑或伪造。

### 已确认根因

- Platform 的实例创建接口允许调用方手工提交全部身份字段。
- 省略实例 ID 时，后端仅生成 `instance-{UUID}`；并未建立稳定的运行时身份协议。
- `applicationId` 和 `environmentId` 在前后端多个入口默认回退到 `app-default`、`env-dev`。
- 数据库迁移预置 `Default Application` 和 `dev` 环境，并被真实联调流程直接复用。
- Attach CLI/Agent 需要显式传入 `platformAgentId`，Agent 不能凭注册凭据自行创建或恢复身份。
- 当前 Agent 心跳主要更新 Agent 记录，未形成 JVM 实例注册、进程身份校验和离线回收的完整闭环。

### 正确产品行为

1. 管理员在 Platform 中创建应用，并为应用启用平台固定的 `DEV`、`SIT`、`UAT`、`PROD` 环境，获得稳定的应用 ID、环境 ID 或注册凭据。
2. Agent 启动时使用受限的注册凭据连接 Platform，不要求操作者预先手工创建实例和 Agent 记录。
3. Agent 自动采集真实运行时元数据并发起注册。
4. Platform 生成不可变的实例 ID 和 Agent ID，并将注册结果返回 Agent。
5. Agent 后续使用获得的身份和短期凭据进行心跳、命令轮询与结果回报。
6. JVM 每次启动具有可区分的进程启动标识；同一部署重启后不得错误复用旧进程状态。
7. Platform 根据心跳租约自动将超时实例标记为离线，不允许向离线或身份不匹配的实例发布规则。
8. 页面优先展示应用名、环境名、主机名和实例显示名；内部 ID 仅在详情或复制操作中展示。

### Agent 自动采集字段

至少包括：

- 真实进程 PID
- 主机名
- JVM 启动时间
- JVM 运行时名称与版本
- Java Vendor
- 操作系统名称、架构和版本
- Agent 版本
- Bootstrap 版本
- 进程主类或启动命令摘要
- Agent 能力列表
- 本地监听地址与端口
- 实例标签
- 唯一启动标识，例如 `processStartId`

不得自动上传 Token、完整 JVM 参数、环境变量、系统属性中的敏感信息或业务密钥。

### 身份生成建议

- `application_id`：由 Platform 创建应用时生成稳定 UUID/ULID；应用名称单独存储。
- `environment_id`：由 Platform 为应用下的固定环境生成稳定 UUID/ULID。
- `environment_type`：平台固定枚举，只允许 `DEV`、`SIT`、`UAT`、`PROD`，不允许用户创建其他环境类型。
- `instance_id`：由 Platform 在 Agent 注册时生成 UUID/ULID。
- `agent_id`：由 Platform 生成，并与实例、安装身份和凭据绑定。
- `process_start_id`：由主机身份、真实 PID、JVM 启动时间和随机启动因子组合或哈希生成，用于识别一次具体 JVM 生命周期。
- Demo、开发和测试数据必须带明确的环境标识，不得与真实环境数据混用。

不应再使用 `app-default`、`env-dev`、`pending`、`unknown` 等值默默替代真实必填信息。缺少关键身份信息时，应拒绝注册并返回明确错误。

### Platform 要求

- 新增 Agent 注册/续租协议，例如：
  - `POST /api/v1/agent-registrations`
  - `POST /api/v1/agents/{id}/heartbeats`
  - `POST /api/v1/agents/{id}/deregister`
- 注册凭据必须限制到指定应用、环境和操作类型，不能直接使用管理员 Token。
- 注册时校验应用、环境关系以及凭据授权范围。
- 支持幂等重试，避免网络抖动产生重复实例。
- 记录首次注册时间、最后心跳时间、租约到期时间和离线原因。
- 心跳更新实例与 Agent 的真实运行状态、版本和能力。
- 定时任务将超过租约的实例标记为 `OFFLINE`。
- 发布目标选择只使用身份有效且租约未过期的实例。
- 对实例重启、Agent 重连、重复注册和凭据撤销提供明确状态迁移。

### Platform Web 要求

- 应用必须通过正式资源管理页面创建；环境只能从该应用下的 `DEV`、`SIT`、`UAT`、`PROD` 中启用和选择，不允许自由填写任意 ID 或创建自定义环境类型。
- 实例列表展示友好名称，并明确区分：
  - 在线
  - 心跳延迟
  - 离线
  - 注册中
  - 身份异常
- 详情页展示真实 PID、JVM 启动时间、JDK、Agent 版本、能力、最后心跳和租约状态。
- 测试/Demo 实例必须带醒目标识。
- 对占位值、缺失值或过期记录显示异常提示，不能伪装为正常 `ACTIVE` 实例。
- 支持查看内部 ID 和复制，但不应把内部 ID 当作主要业务名称。

### 验收标准

- 清空实例和 Agent 数据后启动 `runtime-mock-demo`，无需手工调用实例创建接口。
- Agent 使用应用/环境注册凭据自动完成注册。
- Platform 中出现的新实例具有平台生成的 ID、真实 PID、真实主机名、实际 JDK 版本和 JVM 启动时间。
- Agent 和 JVM 重启后生成新的进程启动身份，旧实例在租约超时后变为“离线”。
- 停止 Agent 后，在配置的租约时间内页面状态自动从“在线”变为“离线”。
- 不传应用、环境或使用越权注册凭据时，注册请求被拒绝。
- 提交 `DEV`、`SIT`、`UAT`、`PROD` 之外的环境类型时，前后端均拒绝请求。
- 发布计划不能选择离线、过期或应用/环境不匹配的实例。
- 页面中不再出现 `process_id=pending` 等占位值被标记为正常实例的情况。
- 自动化测试覆盖首次注册、幂等重试、重启、断线重连、租约超时、越权注册和凭据撤销。

### 应用与环境归属补充约束

Agent 自动接入时不得根据主机名、进程名、包名、启动目录或其他启发式信息猜测应用与环境。

- 应用必须由用户通过注册凭据、启动配置或 Platform 待分配页面，从已有应用中明确选择。
- 环境只能从所属应用已有的 `DEV`、`SIT`、`UAT`、`PROD` 环境中选择。
- 环境选择优先级：
  1. 注册凭据明确绑定环境；
  2. Agent 启动配置明确指定环境；
  3. 均未指定时，`environment_id` 保持为空。
- 未分配环境的实例状态应为 `PENDING_ASSIGNMENT`，页面显示“待分配环境”。
- 待分配实例允许注册、心跳和查看运行时信息，但不得接收规则发布、录制、回放或字节码修改命令。
- 用户在 Web 中完成环境分配后，实例才可进入 `ACTIVE`。
- 注册凭据绑定的环境与启动配置冲突时必须拒绝注册，不得静默覆盖或自动选择。
- 不允许把缺失环境默认写成 `DEV` 或 `env-dev`。

## 4. 发布计划表单使用自由文本，缺少真实资源选择和级联约束

### 状态

已完成（2026-06-20）。

### 问题表现

当前“创建发布计划”表单将以下关联字段设计成自由文本或数字输入：

- 应用 ID
- 环境 ID
- 规则 ID
- 规则版本

用户需要手工复制内部 ID，容易产生前后空格、拼写错误、跨应用错选、环境错配、规则不存在或版本不存在等问题。当前已经出现规则 ID 前多出空格，导致发布计划引用错误资源的实际案例。

这种交互也暴露了大量内部技术字段，使正式产品看起来像接口调试工具。

### 正确产品行为

所有关联已有资源的字段都必须通过 UI 组件从后端真实数据中选择，不允许用户自由输入内部 ID。

创建发布计划时采用级联选择：

1. 选择应用。
2. 根据应用加载其已启用环境。
3. 选择 `DEV`、`SIT`、`UAT` 或 `PROD` 环境。
4. 选择资源类型。
5. 根据应用、环境和资源类型加载可发布资源。
6. 选择规则。
7. 根据规则加载真实存在且允许发布的版本。

后续字段必须受前置选择约束；前置选择改变时，应清空所有不再有效的后续选择。

### 下拉框展示规范

下拉选项必须同时展示用户可理解的名称和可区分的 ID。

规则示例：

```text
测试规则（3092148038204）
```

统一格式：

```text
{资源名称}（{资源 ID}）
```

适用于：

- 应用
- 环境
- 规则
- 录制规则
- 数据集
- 回放计划
- 实例
- Agent
- 审批主体
- 其他引用已有资源的表单字段

界面提交时仅提交选项对应的真实 ID，不得解析展示文本获得 ID。

对于 UUID 较长的资源：

- 默认显示名称和缩略 ID。
- 悬停或详情区域显示完整 ID。
- 提供复制完整 ID 的操作。
- 搜索应同时支持名称、完整 ID 和 ID 前缀。

### 应用选择

- 数据来源必须是 Platform 中已创建且当前用户有权限访问的应用。
- 不得默认填入 `app-default`。
- 无可用应用时显示空状态，并引导用户先创建应用或申请权限。
- 应用选项建议显示为：

```text
订单服务（3092148038001）
```

### 环境选择

- 环境不是自由输入字段。
- 环境类型固定为 `DEV`、`SIT`、`UAT`、`PROD`。
- 只能展示当前应用已经启用且当前用户有权限访问的环境。
- 不得默认填入 `env-dev`，也不得自动猜测环境。
- 环境选项建议显示为：

```text
开发环境 DEV（3092148038101）
系统集成测试 SIT（3092148038102）
用户验收测试 UAT（3092148038103）
生产环境 PROD（3092148038104）
```

- 若实例尚未分配环境，应在实例管理中先完成分配，不能在发布计划中绕过分配直接选择该实例。

### 资源类型选择

- 使用中文名称展示，提交稳定英文代码。
- 示例：
  - `RULE` → 规则
  - `RECORDING_RULE` → 录制规则
  - `DATASET_VERSION` → 数据集版本
- 资源类型改变时必须清空规则/资源和版本字段并重新查询。

### 规则选择

- 数据来源必须是真实规则列表。
- 根据已选择的应用和环境过滤。
- 只显示当前用户有权发布的规则。
- 选项至少展示规则名称和规则 ID：

```text
测试规则（3092148038204）
```

- 可额外展示草稿状态、最新版本、更新时间和目标方法，辅助用户区分同名规则。
- 禁止用户直接输入任意 `ruleId`。

### 规则版本选择

- 版本列表必须来自所选规则已有的 `rule_version` 记录。
- 不应使用可随意增减的数字步进器。
- 只展示满足发布条件的版本，例如校验通过且未被删除的版本。
- 选项建议展示：

```text
版本 3 · 当前草稿 · 更新于 06/19 23:05
版本 2 · 已发布
版本 1 · 已归档
```

- 默认可选中推荐版本，但必须让用户明确看到最终发布版本。

### 后端约束

前端下拉选择不能代替后端校验。创建发布计划时后端必须再次确认：

- 应用存在且用户有权限。
- 环境存在、类型合法且属于所选应用。
- 环境类型只允许 `DEV`、`SIT`、`UAT`、`PROD`。
- 资源存在且属于所选应用和环境。
- 资源版本存在且属于所选资源。
- 资源状态允许发布。
- ID 去除首尾空白后必须与原始输入一致；发现空白或非法格式应直接拒绝。
- 不得通过 `app-default`、`env-dev` 等后端默认值补全缺失的必填关联字段。

### 通用组件要求

- 建立统一的异步资源选择组件，不为每个页面重复实现。
- 支持搜索、防抖、加载状态、空状态、错误重试和分页。
- 支持名称与 ID 联合展示。
- 支持键盘操作和无障碍属性。
- 支持依赖字段变化后的自动清空与重新加载。
- 已失效或无权限资源不得继续作为可选项；编辑历史记录时应明确标记“已失效”。

### 验收标准

- 创建发布计划时，应用、环境、资源、规则和版本均不能自由输入 ID。
- 应用下拉数据来自真实应用列表。
- 环境只显示所选应用下的 `DEV`、`SIT`、`UAT`、`PROD`。
- 规则下拉项按“名称（ID）”展示，例如“测试规则（3092148038204）”。
- 更换应用后，环境、规则和版本自动清空并重新加载。
- 更换环境或资源类型后，规则和版本自动清空并重新加载。
- 规则版本只能从真实版本记录中选择，不能通过数字步进器构造不存在的版本。
- 输入或请求中带有前后空格、跨应用环境、跨规则版本等非法关联时，后端拒绝创建。
- 无应用、无环境、无规则和无版本时均有明确中文空状态和下一步指引。
- 自动化测试覆盖级联选择、搜索、分页、权限过滤、失效资源和后端关联完整性校验。

## 5. 当前阶段发布审批必须允许申请人自审批

### 状态

已完成（2026-06-20）。

### 当前产品决策

Runtime Mock 当前处于开发和能力验证阶段，为避免审批人账号、权限配置和多人协作成为本地联调及功能验证的阻塞项，发布申请人必须能够审批自己发起的发布。

当前所有环境统一允许自审批：

- `DEV`
- `SIT`
- `UAT`
- `PROD`

这是当前阶段的明确产品规则，不应再由后端返回 `SELF_APPROVAL_FORBIDDEN`，也不应要求必须配置另一位审批人。

未来进入正式生产治理阶段后，可以单独将 `PROD` 调整为禁止自审批，但不能现在提前硬编码该限制。

### 当前问题

后端当前存在以下限制：

- 创建审批时禁止把申请人加入审批人列表。
- 审批通过时禁止申请人批准自己发起的审批。
- 发布计划从 `WAITING_APPROVAL` 进入 `APPROVED` 时要求存在另一位用户作出的有效审批决定。

这导致单用户部署、本地环境和当前产品阶段无法完成完整发布闭环。

### 正确产品行为

1. 用户创建发布计划并提交审批。
2. 系统创建与当前发布计划及其版本绑定的审批单。
3. 当前登录用户可以直接点击“批准并发布”。
4. 后端记录申请人和审批人为同一用户的审批决定。
5. 审批通过后，发布计划继续进入发布执行流程。
6. 整个过程保留完整审计记录，不因允许自审批而跳过审批数据。

允许自审批不等于取消审批流程。系统仍必须保存：

- 审批申请
- 审批主体及版本
- 审批决定
- 操作者
- 决定时间
- 审批原因
- 审计记录

### 后端要求

- 删除或按策略关闭 `SELF_APPROVER_FORBIDDEN` 和 `SELF_APPROVAL_FORBIDDEN` 限制。
- 创建审批时允许申请人作为审批人。
- 单用户场景可以默认将当前申请人设为审批人。
- 审批决定仍需绑定准确的主体类型、主体 ID、主体版本和内容哈希。
- 自审批必须经过正常的审批 API 和状态迁移，不能直接把发布计划强制改为 `APPROVED`。
- 增加可配置审批策略，为以后收紧生产环境留出扩展点，例如：

```text
approval.self-approval.DEV=true
approval.self-approval.SIT=true
approval.self-approval.UAT=true
approval.self-approval.PROD=true
```

- 当前默认值全部为 `true`。
- 将来如把 `PROD` 调整为 `false`，后端必须要求不同用户审批，并返回明确中文提示。

### Platform Web 要求

- 审批中心允许申请人看到并操作自己发起的待审批事项。
- 发布详情提供“批准并发布”入口。
- 单用户场景无需手工填写另一位审批人。
- 自审批时明确提示“你正在审批自己发起的发布”，但不阻止操作。
- 审批成功后自动刷新发布计划状态并继续显示后续执行进度。
- 不再显示“申请人不能审批自己的申请”等与当前策略冲突的错误。

### 验收标准

- 用户 `system` 创建发布计划并发起审批后，可以使用同一账号批准。
- 审批记录中的 `requester` 和审批决定 `actor` 可以同时为 `system`。
- 审批通过后发布计划能够从 `WAITING_APPROVAL` 正常进入 `APPROVED`。
- `DEV`、`SIT`、`UAT`、`PROD` 当前均允许自审批。
- 自审批后仍存在完整审批决定和审计记录。
- 不再返回 `SELF_APPROVER_FORBIDDEN` 或 `SELF_APPROVAL_FORBIDDEN`。
- 自动化测试覆盖单用户自审批、版本绑定、重复审批、审批后资源变化及未来关闭自审批策略的场景。
