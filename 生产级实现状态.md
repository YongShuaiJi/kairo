# 生产级实现状态

更新时间：2026-06-18 21:34 Asia/Shanghai

本文档记录 `/Users/jiyongshuai/Desktop/runtime-mock-production-spec` 与当前工程 `/Users/jiyongshuai/code/runtime-mock` 的实现状态。当前项目已从早期七阶段 MVP 扩展为包含生产控制面、Redis fencing、Agent 命令队列、rollout executor、数据抽取 worker 和 replay worker 的多模块工程；OIDC、mTLS/WORM、长稳矩阵和供应链治理仍需继续完成。

---

# 2026-06-18 安全与一致性修复

根据 `BUG-REPORT.md` 对可复现问题逐项核验后，已完成：

- Agent API 禁止空 token，HTTP 仅允许绑定 loopback；Attach CLI 同步拒绝非回环地址。
- Agent token 具备 15 分钟过期校验、半 TTL 自动轮换和 token 文件原子替换；轮换失败时旧 token 到期后 fail-closed。
- Agent 增加 ACTIVE/DISABLED/RESETTING/DEGRADED/STOPPING/STOPPED 状态；降级时卸载 Bridge、关闭规则分发并拒绝新发布。
- reset-class 改为逐规则执行并返回 removed/failed/remaining 状态，失败类进入降级记录，不再因首个异常中断全部清理。
- Groovy 拦截 `GroovyShell`、`GroovyClassLoader`、Instrumentation、Byte Buddy 等逃逸路径，限制危险方法、属性和代码块嵌套。
- Groovy 规则在独立有界执行器中运行，常规执行 100ms 截止；首次执行提供有限冷启动窗口，实际慢执行、连续慢执行和连续错误会自动 LOCKED。
- Groovy classloader 按 generation 轮换，避免脚本版本持续增长导致单一 classloader 永久持有全部生成类。
- Java 属性路径访问禁止 `class`、`classLoader`、反射、Instrumentation、Groovy/Byte Buddy 等敏感对象遍历。
- Agent command 协议身份必须同时满足 agent id 和 identity source；ack 只允许从 `DISPATCHED` 状态执行。
- 平台远程请求必须携带配置的共享 Bearer 凭证；未配置凭证时仅允许 loopback 开发访问，避免远程裸 `X-Actor` 冒充。
- 数据抽取不再接受任意 SQL，只允许校验后的表、列和参数化 where；限制最大行数、超时并使用只读事务。
- Extraction worker 完成/失败更新增加 expected status/version，避免覆盖并发状态。
- Replay worker claim/完成/失败增加 expected status/version；数据库 fencing 序号改为原子递增并校验 token owner。
- Replay worker 增加连续失败和错误率阈值自动暂停；Rollout 失败可触发自动回滚。
- Rollout batch/execution/operation 的关键更新增加数据库 version 字段、expected status/version 和 affected-row 校验，避免更新失败仍记录成功审计。
- Sidecar WAL 增加 1MB 事件上限、10GB 配额、SHA-256 去重、异步落盘和关闭时密钥销毁；脱敏覆盖数组、Set 与 Queue。
- RecordingSession 默认 TTL 改为 900 秒并限制最大 7200 秒；RecordingRule 默认采样率改为 0.001。
- 审批必须显式指定非请求人的 approver，拒绝重复决策和非法 decision。
- PostgreSQL 审计哈希链写入使用事务级 advisory lock 串行化。

仍未完成：

- OIDC/JWT 和 Agent mTLS 身份体系；共享 Bearer 只是过渡边界，header 身份模式仍只能用于 loopback 开发或受信代理后方。
- Groovy LOCKED 后的平台解锁与重新审批闭环，以及更完整的 AST/返回对象复杂度限制。
- BUG 报告中其余资源级 RBAC、独立数据集 DEK/KEK、外部对象存储等生产级差距。

---

# 一、当前验证结果

## 全量测试

命令：

```bash
mvn test
```

结果：

- Reactor：18 个模块全部 SUCCESS。
- 最终时间：2026-06-18 12:03:51 +08:00。
- Agent 集成测试：24 个通过。
- 平台集成测试：3 个场景通过（控制面端到端、自动化 worker、安全默认值与审批约束）。
- Flyway V1/V2/V3 全部迁移成功。
- 集成测试覆盖：实例、Sidecar、Agent、心跳、Agent command poll/ack、规则、规则版本、发布计划、批次、实例执行、rollout executor、录制规则、录制会话、Dataset、数据源、抽取模板、抽取任务、抽取 worker、ReplayPlan、ReplayExecution、replay worker、审批、审计 hash chain、outbox。

## 全量打包

命令：

```bash
mvn -DskipTests package
```

结果：

- Reactor：18 个模块全部 SUCCESS。
- 最终时间：2026-06-17 09:55:36 +08:00。
- `runtime-mock-platform-server` 已通过 Spring Boot repackage 生成可执行 jar。

## Docker 验证

命令：

```bash
docker compose config
docker compose build platform
docker compose up -d postgres kafka platform
./scripts/platform-smoke.sh
```

结果：

- `runtime-mock-postgres` healthy。
- `runtime-mock-kafka` healthy。
- `runtime-mock-platform` healthy。
- `GET http://127.0.0.1:18280/api/v1/control/health` 返回 `status=UP` 且 `storage=postgresql`。
- 冒烟脚本成功创建 `smoke-instance` 和 `smoke-rule`。
- outbox 事件已由 Kafka publisher 标记为 `PUBLISHED`，`attempts=1`。

---

# 二、当前模块

```text
runtime-mock-bootstrap-api
runtime-mock-api
runtime-mock-object
runtime-mock-groovy
runtime-mock-core
runtime-mock-agent-core
runtime-mock-agent-server
runtime-mock-agent-core-modern
runtime-mock-agent-bootstrap
runtime-mock-attach-cli
runtime-mock-ops
runtime-mock-sidecar
runtime-mock-web
runtime-mock-control-server
runtime-mock-platform-server
runtime-mock-demo
runtime-mock-integration-tests
```

---

# 三、已完成的生产化增量

## 1. Agent 打包隔离

已完成：

- `runtime-mock-agent-bootstrap` 是 thin bootstrap。
- `runtime-mock-agent-core-modern` 是隔离加载的 modern core jar。
- Bootstrap 通过反射加载 Core Launcher，避免业务进程直接污染 bootstrap 依赖。
- Bootstrap 与 bootstrap-api 编译为 Java 8 字节码。
- 本机 registration/token 文件已写入运行目录。

仍缺：

- JDK 8/11 legacy core 产物和 CI 矩阵。
- Core jar 签名校验与供应链验签。

## 2. Agent 本地 API 与 Ops

已完成：

- Agent API 支持 `/v1`。
- 已实现 status、class/method discovery、script compile、rule CRUD、metrics/events、disable-all、enable-all、reset-class、reset-all、shutdown。
- 除 health 外要求 Agent token。
- `runtime-mock-ops` 支持本地应急操作。

仍缺：

- Token 15 分钟轮换和 Sidecar 下发闭环。
- Ops 操作与生产审批/RBAC 的联动。

## 3. Groovy 安全

已完成：

- 接入 `SecureASTCustomizer`。
- 禁止危险 import/static import/star import。
- 禁止文件、网络、反射、线程、进程、Runtime/System/ClassLoader 等危险 receiver/type。
- 禁止 package、自定义方法、循环、synchronized、class/classLoader/metaClass 等。
- 增加脚本字符数、行数、闭包/语句文本长度限制。

仍缺：

- 对不响应线程中断的恶意脚本进行进程级强隔离。
- 更完整的 AST/返回对象复杂度指标和 LOCKED 重新审批流程。

## 4. Spring Boot 平台控制面

已完成：

- 新增 `runtime-mock-platform-server`。
- Spring Boot 3 / Java 21。
- PostgreSQL/Flyway 生产持久化，H2 PostgreSQL mode 测试。
- V1 核心表：组织资产、用户/角色/权限、RecordingSession、DatasetVersion、ReplayPlan、Approval、Audit、Outbox。
- V2 控制面表：Instance、Sidecar、Agent、规则、规则版本、发布计划、批次、实例执行、回滚、观察窗口、健康信号。
- V3 数据表：RecordingRule、补充录制索引、Dataset schema/manifest/object reference、Datasource、ExtractionTemplate/Task、ReplayExecution、Comparison/Cleanup。
- RBAC capability 校验。
- 审批请求、审批步骤、审批决策。
- 状态机均使用 expectedStatus + expectedVersion + fencingToken。
- Redis-backed fencing token 已接入，Docker Compose 中通过 `RUNTIME_MOCK_FENCING_REDIS_ENABLED=true` 使用 Redis 发号，PostgreSQL 保存最终审计和 token 状态。
- 审计记录为 hash chain。
- 数据库 outbox 写入所有写操作事件。
- Kafka outbox publisher 可通过 `RUNTIME_MOCK_KAFKA_ENABLED=true` 启用。
- V5 表：`agent_command`、`worker_artifact`。
- Agent command 队列支持手工下发、Agent poll/lease、ack/fail、rollout execution 状态回写。
- Rollout executor 支持 RUNNING operation plan 扫描、批次启动、目标快照、Agent command 幂等下发、ack 后推进 execution/batch/operation。
- Extraction worker 支持 sampleRows/JDBC 只读抽取、`extraction_execution`、`extraction_result` 和 artifact 写入。
- Replay worker 支持 synthetic/HTTP target 回放、`replay_batch`、`replay_invocation_result`、`comparison_result` 和 summary artifact。

仍缺：

- OIDC/JWT/企业 SSO 尚未真实接入，目前使用 `X-Actor` 本地用户。
- MyBatis-Plus 未引入，当前使用 Spring JDBC 显式 SQL。
- WORM/Object Lock 审计归档未接入。
- Rollout 观察窗口和自动回滚策略已建模，执行器仍需继续扩展完整策略判断。

## 5. Docker 与运维脚本

已完成：

- `docker-compose.yml` 包含 PostgreSQL、Kafka/Redpanda、Redis、MinIO、Keycloak、platform。
- `runtime-mock-platform-server/Dockerfile`。
- `scripts/platform-up.sh`。
- `scripts/platform-down.sh`。
- `scripts/platform-smoke.sh`。
- `docs/ops/platform-docker.md`。

## 6. API 与治理文档

已完成：

- `docs/api/platform-openapi.yaml`。
- `docs/api/permission-matrix.md`。
- `docs/api/error-codes.md`。
- README 已更新平台模块、启动方式、主要 API。

## 7. Sidecar 数据安全底座

已完成：

- `runtime-mock-sidecar`。
- 脱敏、hash、tokenize、generalize、fixed、preserve-format。
- HMAC 稳定 tokenization。
- AES-GCM 加密 WAL。

仍缺：

- gRPC mTLS 服务。
- Agent bounded queue 到 Sidecar 的录制链路。
- WAL quota 与对象存储上传。
- STS/预签名凭证和 token 轮换。

---

# 四、差距矩阵

| 规格域 | 当前状态 | 剩余差距 |
| --- | --- | --- |
| Agent 隔离 | thin bootstrap + modern core 已实现 | legacy core、签名校验、JDK 8/11 矩阵 |
| Agent API | `/v1` 本地 API、Ops CLI、token 过期与自动轮换已实现 | Sidecar 管理闭环、mTLS |
| Groovy 安全 | AST 限制、运行时截止、慢执行/错误自动 LOCKED、classloader generation 已实现 | 进程级隔离、复杂度、解锁重审 |
| 控制平面 | Spring Boot + PostgreSQL/Flyway + Redis fencing + RBAC + 审批 + 审计 + outbox | OIDC、WORM 归档 |
| 资产/Agent | instance、sidecar、agent、heartbeat、agent command poll/ack、token 轮换已实现 | mTLS/gRPC 注册 |
| 规则/发布 | rule/version、operation plan、rollout batch/execution、版本并发保护、自动回滚已实现 | 更完整观察窗口与回滚策略 |
| Record | recording rule/session/schema/index 元数据已持久化 | Agent 事件采集到 Sidecar，再到对象存储 |
| Dataset | dataset version/schema/manifest/object reference 元数据已持久化 | Parquet 打包 worker、生命周期删除执行 |
| Data Extractor | datasource/template/task 状态机与 worker 已实现 | SQL 计划检查、MySQL driver 打包、扩缩容 |
| Replay | replay plan/execution、worker、基础比较结果已实现 | Replay SDK 丰富协议、清理执行器 |
| Kafka | outbox publisher 已实现 | topic 初始化、消费端幂等表使用、告警通知 |
| Docker | 本地依赖和 platform compose 已完成 | Helm/CloudNativePG/Patroni/Vault/PKI |
| 测试 | 全量 `mvn test`、平台自动化集成测试、Docker Redis/Kafka/PostgreSQL smoke 已通过 | JDK/OS 矩阵、性能、长稳、故障注入 |

---

# 五、当前结论

当前工程已经具备生产平台的主要控制面和基础执行闭环：数据库模型、API、状态机、RBAC、审批、Redis fencing、Agent command、rollout executor、数据抽取 worker、replay worker、审计、outbox、Docker 运行面和核心文档已落地。

但按“100% 生产级完整产品”口径，仍不能宣称全部完成。剩余关键工作集中在生产硬化和企业集成：Sidecar gRPC/mTLS、录制队列到对象存储上传、自动回滚完整策略、OIDC/SSO、WORM 归档、供应链签名/SBOM、Helm/PKI/Vault，以及完整兼容性、性能和长稳测试矩阵。
