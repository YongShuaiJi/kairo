# Runtime Mock 当前实现与交付状态

更新时间：2026-06-19 Asia/Shanghai

## 1. 交付结论

按当前确认的产品范围，Runtime Mock 已形成可独立部署和完整操作的产品：

- `runtime-mock-platform-web`：Next.js 15、React 19、TypeScript、Tailwind、shadcn/ui、
  Lucide、TanStack Query 和 Monaco Editor 的独立中央管理前端。
- Platform API：本地 opaque Bearer Token、RBAC、审计、控制面、脚本工作台、聚合查询、
  详情/分页查询和录制接入。
- Platform Worker：Rollout、Extraction、Replay、Outbox 和加密对象归档。
- Runtime Agent：字节码增强、规则执行、命令轮询、自动录制和有界批量上传。
- PostgreSQL、Redis、Kafka、MinIO：分别承担权威状态、协调、异步事件和加密大对象。

Kubernetes、企业 SSO、Vault、云 KMS、WORM、多区域和云对象存储适配器是后续可选集成，
不属于当前产品运行依赖，也不计为未关闭 BUG。

## 2. 已完成的产品闭环

### 规则与发布

- 支持 BEFORE、RETURN、THROWS 规则和不可变版本。
- Monaco 工作台支持 Groovy 编辑、补全、服务端安全校验、受控试运行、结果/异常/耗时展示。
- 发布计划、批次、实例执行、Agent command、ack、状态推进和自动回滚已打通。
- 规则注册、状态机与执行更新使用版本条件、幂等和 fencing 保护。

### Agent 与运行时安全

- Thin Java 8 bootstrap + 隔离 modern core；支持 premain 和 dynamic attach。
- 本地 Agent API 强制 loopback 和 Token；Token 具有过期、自动轮换与原子文件更新。
- Agent 具有 ACTIVE、DISABLED、RESETTING、DEGRADED、STOPPING、STOPPED 状态。
- Groovy 具有 AST/类型/属性路径限制、有界执行时间、连续慢执行/错误自动 LOCKED、
  classloader generation 轮换和业务 fail-open。
- Runtime Observer 在不改变规则有效返回/抛出结果的前提下采集调用。

### 录制与数据集

- 录制会话状态变化自动向目标 Agent 下发 `START_RECORDING` / `STOP_RECORDING`。
- Agent 使用有界队列、批量、超时和重试上传录制事件。
- Platform 校验 Agent 身份和会话状态，执行脱敏、事件/批次限制和去重。
- 每个对象使用随机 DEK + AES-256-GCM 加密，DEK 由本地 KEK 包装后写入 MinIO。
- PostgreSQL 保存录制批次、事件索引、哈希、对象引用和加密元数据，不保存大型明文。
- 成功录制会话可生成不可变 Dataset；Extraction 结果也可生成带来源链路的 Dataset。

### 数据提取与回放

- 数据源、模板、任务、执行和结果对象已持久化。
- JDBC 提取只允许校验后的表/列/参数化 where，使用只读事务、超时和行数上限。
- Replay 支持 synthetic/HTTP target、批次、调用结果、比较结果、摘要对象和错误率自动暂停。
- Worker 产物通过统一 ObjectStorage SPI 加密写入 MinIO，不写平台本地目录。

### 身份、审计与 Web

- 平台使用 256-bit 随机 opaque Token，数据库只保存哈希，支持到期、撤销和最后使用时间。
- `/auth/me` 返回当前主体、角色、权限范围和 Token 元数据；前端会话使用加密 HttpOnly Cookie。
- 审计记录使用 PostgreSQL advisory lock 串行写入哈希链。
- Web 已覆盖总览、Agent、规则、发布、录制/数据集、提取、回放/比较、审批、审计和系统管理。
- 真实模式不使用假数据兜底；Demo 模式仅用于隔离前端开发和 E2E。

## 3. 模块与运行单元

Maven/发行模块：

```text
runtime-mock-bootstrap-api
runtime-mock-api
runtime-mock-object
runtime-mock-groovy
runtime-mock-core
runtime-mock-agent-core
runtime-mock-agent-server
runtime-mock-agent-core-modern
runtime-mock-agent-bootstrap
runtime-mock-attach-cli
runtime-mock-ops
runtime-mock-sidecar
runtime-mock-storage-spi
runtime-mock-storage-minio
runtime-mock-platform-server
runtime-mock-demo
runtime-mock-integration-tests
```

独立前端工程：

```text
runtime-mock-platform-web
```

默认运行单元只有 Platform Web、Platform API、Platform Worker 和嵌入目标 JVM 的 Runtime
Agent。API 与 Worker 共享同一后端产物但按角色独立启动；`runtime-mock-sidecar` 是数据安全
领域库，不要求部署成单独服务。

## 4. 数据库与基础设施

- Flyway V1–V8：核心模型、资产/规则/发布、录制/提取/回放、fencing、Agent command、
  Worker artifact、并发审计字段、本地 Token、Extraction→Dataset 来源链路。
- Redis：fencing sequence。
- Kafka/Redpanda：Transactional Outbox 至少一次发布。
- MinIO：录制、Extraction 和 Replay 加密对象。
- Docker Compose：PostgreSQL、Redis、Redpanda、MinIO、Platform API、Platform Worker、
  Platform Web 共 7 个健康容器。

## 5. 验收基线

完整验收命令：

```bash
mvn test

cd runtime-mock-platform-web
npm run api:generate
npm run typecheck
npm run lint
npm test
npm run build
npm run test:e2e

cd ..
./scripts/platform-up.sh
./scripts/platform-smoke.sh
```

Docker 冒烟覆盖 9 个阶段：健康/认证/脚本、实例与 Agent 身份、发布命令、录制上传与 Dataset、
Extraction 与来源链路、Replay、Web 查询契约、Kafka/Redis/MinIO/PostgreSQL，以及 MinIO
密文不包含明文敏感数据。

2026-06-19 最终回归结果：

- Maven Reactor 18/18 `SUCCESS`；Agent 集成测试 25/25，Platform 集成测试 9/9。
- OpenAPI 55 条路径解析及 TypeScript 类型生成成功。
- 前端 TypeScript、ESLint、Vitest、20 路由生产构建和 Playwright 核心流程全部通过。
- Compose 7 个容器全部 healthy；产品冒烟 9/9 通过，最终 smoke ID 为 `1781823198`。
- Chrome 真实环境完成登录、总览、规则编辑器、服务端校验、试运行、录制批次和事件索引验收，
  控制台无 warning/error。

## 6. 当前边界与后续增强

以下事项不影响当前产品完整运行，但在更高风险或更大规模环境中应继续增强：

- Groovy 进程级隔离、LOCKED 后审批化解锁和更细的对象复杂度预算。
- JDK 8/11 legacy core、更多 ClassLoader/JDK/OS 兼容矩阵与供应链签名。
- OIDC/mTLS、云 KMS、阿里云 OSS/腾讯 COS/华为 OBS 适配器。
- MySQL 驱动打包、SQL 计划审查、分布式 Worker 扩缩容和更丰富比较策略。
- 性能、长稳、故障注入、跨区域容灾和 WORM 合规认证。

这些是产品演进项，不是对当前 agreed scope 的功能缺口。
