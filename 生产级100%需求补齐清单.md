# 生产级 100% 需求补齐清单

本文档用于补齐 Runtime Mock 从当前 MVP 基线走向“生产级完整产品”所需的业务决策、使用场景、配置策略、验收标准和配套文档。

当前工程已经完成七阶段 MVP 与核心验收场景，但原始三合一文档仍不足以定义“100% 生产级完成”。原因是生产级产品不仅需要功能可运行，还需要明确安全边界、权限审批、部署架构、集群治理、自动回滚、审计合规、运维应急和验收指标。

下面清单中的内容需要在继续生产化开发前逐项明确。

## 范围调整说明（2026-06-18）

本清单保留为早期“全企业级能力”差距台账，不再作为当前阶段的逐项验收范围。
当前确认的产品边界以
`docs/requirements/runtime-mock-product-requirements.md` 和
`docs/architecture/simplified-platform-architecture.md` 为准。

当前阶段保留 PostgreSQL、Redis、Kafka 和 MinIO，明确不把 Kubernetes、企业 SSO、
Vault、云 KMS、WORM 合规和多区域部署作为交付前提。相关条目并非永久删除，而是作为
可选企业适配进入后续阶段。

## 当前交付状态（2026-06-19）

按上述范围调整后的产品基线，Platform Web、Platform API、Platform Worker、Runtime Agent、
PostgreSQL、Redis、Kafka 与 MinIO 已完成实现和端到端联调。本清单后续条目继续作为企业级
扩展与长期硬化参考，不应再被统计为“49 项未解决 BUG”，也不构成当前完整产品的交付阻塞。

---

# 一、生产支持范围

需要补齐：

- 支持哪些 JDK 版本：只支持 Java 17，还是同时支持 Java 8、11、17、21。
- 支持哪些运行环境：Spring Boot fat jar、Tomcat、Dubbo、普通 Java 进程、Docker、Kubernetes。
- 支持哪些 ClassLoader 场景：AppClassLoader、Spring Boot LaunchedClassLoader、Tomcat WebAppClassLoader、OSGi、自定义插件 ClassLoader。
- 原 MVP 暂不支持项在生产级是否仍然不支持，还是需要纳入开发范围。

需要明确的关键决策：

- 构造方法是否支持。
- 类初始化方法是否支持。
- native 方法是否支持。
- abstract 方法是否支持。
- JDK 核心类是否允许增强。
- Bootstrap ClassLoader 业务类是否允许增强。
- Lambda hidden class 是否需要特殊处理。
- CompletableFuture、Reactor、Kotlin suspend、Scala Future 是否纳入生产范围。
- 跨方法状态编排是否纳入生产范围。

补齐后用于：

- 定义兼容性测试矩阵。
- 确定 Byte Buddy 匹配和排除策略。
- 确定文档中“支持”和“不支持”的正式边界。

---

# 二、部署架构

需要补齐：

- 控制端部署方式：单机、主备、集群。
- Agent 与控制端通信方式：控制端主动访问 Agent，还是 Agent 反向注册到控制端。
- Agent HTTP 是否只监听 `127.0.0.1`，还是允许远程访问。
- 是否需要 Sidecar 或本机代理。
- 是否支持 Kubernetes 内通过 Pod、Service、Label 发现 JVM。
- 是否接入 CMDB、Nacos、Consul、Eureka 或其他服务发现系统。

需要明确的关键决策：

- Agent 是否必须默认本地监听。
- 跨机器访问 Agent 是否必须 TLS/mTLS。
- 控制端如何发现目标 JVM。
- Agent 端口冲突时如何处理。
- Agent 启动失败、重复加载、版本不一致时如何处理。

补齐后用于：

- 设计 Agent 注册中心。
- 设计控制端 JVM 管理模型。
- 设计部署文档和运维手册。

---

# 三、权限与审批

需要补齐：

- 用户体系：本地账号、LDAP、OIDC、企业 SSO。
- 角色体系：管理员、开发、测试、只读、审批人、应急操作员。
- 权限粒度：按应用、环境、JVM、类、方法、规则动作控制。
- 生产环境规则是否必须审批。
- 审批级数：一级审批、多人审批、按风险等级审批。
- 紧急操作是否允许跳过审批。
- 谁可以执行 disable-all、reset-all、shutdown-agent。

需要明确的关键决策：

- 创建规则、编辑规则、编译脚本、发布规则、禁用规则、删除规则分别需要什么权限。
- 生产环境是否允许个人直接发布。
- 审批通过后是否允许修改脚本。
- 审批单是否需要过期。
- 审批记录是否需要不可篡改。

补齐后用于：

- 设计用户、角色、权限数据模型。
- 设计审批流。
- 设计审计日志。
- 设计控制台页面。

---

# 四、Groovy 安全边界

需要补齐：

- 生产环境是否允许执行 Groovy。
- 是否区分测试、预发、生产的 Groovy 能力。
- 允许 import 的包列表。
- 禁止 import 的包列表。
- 禁止访问的类型、方法、字段和注解。
- 是否允许反射。
- 是否允许文件系统访问。
- 是否允许网络访问。
- 是否允许创建线程。
- 是否允许使用 ClassLoader。
- 是否允许动态依赖下载。
- 是否允许访问系统属性和环境变量。
- 是否需要脚本执行超时。
- 是否需要限制脚本创建对象数量、集合大小、字符串长度。

需要明确的关键决策：

- `System.exit`、`Runtime.exec`、`ProcessBuilder`、`Thread`、`Socket`、`File`、`ClassLoader`、`@Grab` 的正式策略。
- 是否强制使用 `SecureASTCustomizer`。
- 是否使用 `ImportCustomizer`。
- 是否编写自定义 AST 校验。
- 是否为不同环境配置不同安全等级。

补齐后用于：

- 设计 Groovy 安全策略。
- 增加危险脚本测试集。
- 明确“不是完整沙箱”的正式产品说明。

---

# 五、规则治理策略

需要补齐：

- TTL 默认值和最大值。
- `maxHits` 默认值和最大值。
- 命中比例默认值和最大值。
- 生产环境是否允许 100% 命中。
- 是否支持稳定采样键：Trace ID、用户 ID、请求 ID。
- 是否允许修改入参。
- 是否允许提前返回。
- 是否允许吞异常。
- 是否允许把正常返回转异常。
- 是否允许返回假数据。
- 是否按应用、环境、包名、类名、方法配置 allowlist / denylist。

需要明确的关键决策：

- 高风险规则是否必须审批。
- 高风险动作是否只能在测试环境使用。
- 规则是否必须设置 TTL。
- 规则是否必须设置 maxHits。
- 命中比例是否必须从小比例灰度开始。

补齐后用于：

- 设计规则风险等级。
- 设计发布前校验。
- 设计控制台表单约束。
- 设计生产保护策略。

---

# 六、集群发布与灰度

需要补齐：

- 一条规则发布到多个 JVM 时，全成功才算成功，还是允许部分成功。
- 部分 JVM 发布失败时，是否自动回滚已经成功的 JVM。
- 发布是否支持分批。
- 灰度维度：机器、实例比例、应用标签、机房、环境、流量标签。
- 灰度批次之间是否需要人工确认。
- 灰度批次之间是否需要健康观察窗口。
- 多人同时发布同一方法规则时如何冲突检测。
- Agent 离线时如何处理待发布规则。

需要明确的关键决策：

- 发布计划的数据结构。
- 发布状态的终态：成功、部分成功、失败、已回滚、取消。
- 规则版本冲突处理方式。
- 是否允许同一方法存在多条生产规则。
- 是否允许跨应用批量发布。

补齐后用于：

- 设计集群发布执行器。
- 设计发布计划和发布记录表。
- 设计灰度发布页面。
- 设计回滚逻辑。

---

# 七、自动回滚

需要补齐：

- 自动回滚触发条件。
- 脚本错误数阈值。
- 脚本错误率阈值。
- 业务异常率阈值。
- 接口耗时阈值。
- 命中失败率阈值。
- 外部监控告警是否触发回滚。
- 发布后观察窗口时长。
- 回滚动作类型：disable rule、delete rule、reset class、reset all。
- 回滚是否需要二次确认。
- 回滚是否通知相关人员。

需要明确的关键决策：

- 自动回滚默认是否开启。
- 自动回滚是否只在生产开启。
- 回滚阈值是全局配置还是每条规则单独配置。
- 回滚后规则是否允许再次自动发布。
- 回滚事件是否进入审批和审计。

补齐后用于：

- 设计自动回滚引擎。
- 设计指标采集。
- 设计告警通知。
- 设计发布观察窗口。

---

# 八、持久化与审计

需要补齐：

- 数据库选型：MySQL、PostgreSQL、SQLite、H2、文件存储。
- 是否需要高可用数据库。
- 审计日志保留周期。
- 事件日志保留数量。
- 是否记录调用事件。
- 是否记录参数和返回值。
- 参数和返回值如何脱敏。
- 哪些字段必须屏蔽。
- 审计日志是否要求不可篡改。
- 是否支持审计导出。

建议数据对象：

- 应用。
- 环境。
- JVM 实例。
- Agent 实例。
- 用户。
- 角色。
- 权限。
- 规则。
- 规则版本。
- 发布计划。
- 发布记录。
- 审批单。
- 操作审计。
- 运行事件。
- 指标快照。

补齐后用于：

- 设计数据库 schema。
- 设计审计查询页面。
- 设计合规导出功能。
- 设计数据保留和清理任务。

---

# 九、Agent 包装与配置

需要补齐：

- 是否必须严格实现双 JAR：`bootstrap jar + agent-core jar`。
- Agent Core jar 存放位置。
- Agent Core 版本升级方式。
- 配置来源：agent args、配置文件、环境变量、控制端下发。
- token 生成方式。
- token 保存方式。
- token 轮换方式。
- token 吊销方式。
- 是否支持 TLS/mTLS。
- Agent 端口选择策略。
- Agent 关闭策略。
- Agent 卸载策略。

需要明确的关键决策：

- bootstrap jar 是否绝对不能包含 Groovy、Byte Buddy、Jackson。
- Agent Core 是否允许热升级。
- Agent 加载失败是否影响业务启动。
- Agent HTTP 启动失败是否影响业务启动。
- Agent reset 是否必须恢复所有已增强类。

补齐后用于：

- 改造打包结构。
- 设计 Agent 配置文件。
- 设计升级和回滚文档。
- 设计依赖隔离验收测试。

---

# 十、本机应急命令

需要补齐：

- 本机命令入口形式：单独 ops CLI，还是复用 attach CLI。
- 命令认证方式。
- 如何从 PID 发现 Agent host、port、token。
- 命令返回码。
- 命令错误信息格式。
- 命令执行审计方式。

必须定义的命令：

```text
disable-rule
disable-all
remove-rule
reset-class
reset-all
shutdown-agent
```

每个命令需要明确：

- 参数。
- 成功返回。
- 失败返回。
- 是否幂等。
- 是否允许无控制端执行。
- 是否写入本地审计。

补齐后用于：

- 实现生产应急 CLI。
- 编写应急手册。
- 编写故障演练方案。

---

# 十一、控制台产品细节

需要补齐：

- 控制台是否必须覆盖第 24 节全部能力。
- Groovy 编辑器选型：原生 textarea、CodeMirror、Monaco。
- 是否需要语法高亮。
- 是否需要基础格式化。
- 是否需要 MockApi 自动补全。
- 是否需要错误行定位。
- 是否需要脚本版本 diff。
- 是否需要规则模板管理。
- 是否需要批量发布、批量禁用、批量删除。
- 是否需要按应用、环境、状态、创建人、审批状态筛选。
- 是否需要移动端或只支持桌面端。

需要明确的关键决策：

- 控制台是否是生产主要入口。
- 是否存在只读视图。
- 是否需要审批工作台。
- 是否需要审计导出页面。
- 是否需要集群发布页面。

补齐后用于：

- 设计前端页面。
- 设计控制端 API。
- 设计用户操作流。

---

# 十二、生产验收标准

需要补齐：

- 插桩后平均耗时增加上限。
- p95、p99 耗时增加上限。
- 单 JVM 最大规则数。
- 单方法最大规则数。
- 单 JVM 最大并发调用数。
- 脚本最大执行耗时。
- Agent 最大内存占用。
- 控制端最大管理 JVM 数。
- 集群发布最大实例数。
- 长稳测试时长。
- 兼容性测试矩阵。
- 故障演练场景。

建议验收场景：

- Agent 启动失败不影响业务。
- Agent HTTP 失败不影响业务。
- Groovy 脚本异常 fail-open。
- 发布失败不留下半生效规则。
- 删除失败可回滚。
- reset-all 后业务恢复原行为。
- 控制端不可用时可本机应急关闭规则。
- 高并发下 maxHits 不突破严格限制。
- 高并发下 ThreadLocal 不串数据。
- 多 ClassLoader 同名类只命中指定目标。
- Agent Core 与业务依赖无冲突。
- 权限不足不能发布生产规则。
- 审批未通过不能发布生产规则。
- 自动回滚触发后规则被禁用并记录审计。

补齐后用于：

- 建立生产验收 checklist。
- 建立性能压测计划。
- 建立兼容性测试计划。
- 建立发布准入标准。

---

# 十三、配套文档清单

生产级交付前需要补齐：

- 安装部署文档。
- Agent 参数文档。
- 控制端配置文档。
- HTTP API 文档。
- OpenAPI 规范。
- 用户手册。
- 管理员手册。
- 权限和审批说明。
- Groovy 脚本安全规范。
- 规则编写最佳实践。
- 运维手册。
- 应急手册。
- 故障排查手册。
- 升级文档。
- 回滚文档。
- 兼容性说明。
- 已知限制说明。
- 安全风险说明。

---

# 十四、优先级建议

建议按以下顺序补齐需求和实现：

1. 明确生产支持范围和不支持边界。
2. 明确安全模型和 Groovy 能力边界。
3. 明确 Agent 部署架构和双 JAR 隔离目标。
4. 明确权限、审批、审计模型。
5. 明确本机应急命令。
6. 明确持久化数据模型。
7. 明确集群发布和灰度策略。
8. 明确自动回滚策略。
9. 明确控制台产品细节。
10. 明确性能、兼容性和生产验收指标。

---

# 十五、当前结论

当前 Runtime Mock 工程已经具备 MVP 运行能力，但“生产级 100% 完成”必须先补齐上述决策和材料。否则后续开发会出现以下风险：

- 生产范围不清，导致实现边界不断扩大。
- 安全边界不清，导致 Groovy 风险无法验收。
- 权限审批不清，导致生产使用无法落地。
- 集群和回滚策略不清，导致发布系统无法设计。
- 审计和脱敏要求不清，导致合规风险。
- 验收指标不清，导致无法判断是否真正生产可用。

本清单应作为生产化开发前的需求澄清文档。每一项补齐后，应同步更新实现版三合一文档、README、API 文档、测试计划和运维手册。
