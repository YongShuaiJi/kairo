# V1.7 技术实施方案：可靠收口、稳定验证与 LTS 封版

> 上游基线：`V1.6.0`，tag `V1.6.0`，commit `113823b41981a2d8fb5473a772ae2d2938d9582e`。
> 开发分支：`V1.7`；产品版本：`1.7.0`；Git tag：`V1.7.0`；维护线：`V1.7.x`。
> 当前状态：实施基线。改变目标、支持范围、状态语义或发布门槛前，必须先修改本文并说明原因。
> 版本性质：V1.X 功能封版。V1.7 不继续扩展方法增强能力，只解决可靠运行、真实支持和正式交付。
> LTS 治理：[V1.7 LTS 支持、弃用与回补策略](./v1.7-lts-policy.md)。
> 路线图来源：[V1.X 版本路线图 §9](../v1.x-version-roadmap.md)。

## 文档导航

- §1～§4：版本目标、当前代码事实、正式支持边界和不可违反的可靠性语义；人和开发智能体开工前必读。
- §5～§6：证据模型与 M0～M6 顺序；主验收者维护。
- §7：已完成的 M0 基线事实。
- §8：M1-A～M1-F 恢复工作包；每个 Claude 会话只执行其中一个。
- §9：M2 性能、10,000 cycle、泄漏和 soak。
- §10：M3 精确兼容矩阵。
- §11：M4 health、指标、支持包和 runbook。
- §12：M5 版本、发布物和供应链。
- §13：M6 最终 LTS 认证。
- §14～§16：通用门禁、Claude 执行协议和缺陷分支规则。
- §17～§18：Requirement 映射和最终完成定义。

## 1. 唯一版本目标

V1.1～V1.6 已经完成字节码观察、脚本能力、增强位置、规则链、现代 JVM 目标解析和 API/AI
自动化能力。V1.7 不再回答“还能增强什么”，只回答以下四个发布问题：

1. **不会错做**：重复、延迟、乱序和旧 owner 的命令不能覆盖新状态，完成过的正式操作不会重复执行。
2. **能够恢复**：Platform、Agent 或目标 JVM 重启后，正式规则能够按平台期望状态重新收敛；临时规则不会错误复活。
3. **不会拖垮目标 JVM**：真实增强/卸载循环、脚本和 ClassLoader 生命周期不存在持续资源漂移或不可接受退化。
4. **能够正式交付**：版本、支持矩阵、健康状态、发布物和验收证据真实一致，用户能够判断风险并完成恢复。

V1.7.0 的一句话完成标准是：

> 在本文明确支持的单 Platform 拓扑和 JVM 组合中，Kairo 经受重启、断连、重试、乱序、卸载和长稳验证后，
> 仍能保持方法实际行为、规则状态、字节码 revision/hash 与平台权威状态一致，并能从干净源码生成可验证发布物。

## 2. 当前代码事实

以下内容是实施输入，不允许开发智能体忽略后重新设计一套系统。

### 2.1 已经完成的能力

- `kairo-core` 已有不可变规则集、原子替换、采样、命中上限、重入保护和 fail-open。
- `kairo-agent-core` 已有真实 Instrumentation、规则链 CAS、转换 revision/hash、精确卸载、字节码快照、
  ClassLoader 生命周期清理、代理/Lambda/bridge/synthetic 处理和热更新漂移识别。
- `kairo-agent-server` 已有 Agent 本地 API、平台注册、心跳、命令轮询、协议 capability 和本地紧急操作入口。
- `kairo-platform-server` 已有 PostgreSQL/Flyway 权威状态、Redis/DB fencing sequence、规则版本、发布计划、
  Agent 命令、卸载执行、审计、RBAC、API 幂等记录和 desired/actual 规则链状态表。
- `kairo-platform-web`、`kairo-cli`、`kairo-sdk`、`kairo-mcp` 已共享 Platform API，不需要 V1.7 再建设新操作面。
- M0 已保存 V1.6 OpenAPI、错误码、配置、V1～V41 迁移和 Agent 协议基线，并建立破坏性变更测试。

### 2.2 必须正视的缺口

- `AgentCommandService`/`AgentCommandMapper` 有 attempts 和 lease，但 Agent ACK 尚未完整携带 dispatch epoch；
  旧 ACK 在重新派发后仍可能缺少可靠围栏。
- `PlatformCommandPoller` 的 `REFRESH_RUNTIME_STATE` 目前只返回 `{"refreshed": true}`，没有报告真实规则、
  revision、hash、TTL 或 degraded state。
- `RuleChainStateService` 能比较 desired/actual，却没有驱动自动收敛；目标 JVM 重启后内存规则会丢失。
- `BytecodeDiagnosticExchange`、`ScriptSessionExchange`、`TargetResolutionExchange` 有意只保存临时敏感数据；
  Platform 重启后不能安全恢复这些请求，必须失败得明确，不能假装持久化。
- `PlatformMaintenanceService` 当前会在 Agent 不可达时把部分计划直接写成 `UNLOADED/REMOVED`；Agent 断连不等于
  JVM 已卸载规则，该语义必须纠正。
- 默认部署是一个 Platform 进程。`RolloutExecutor` 的运行锁是本进程 `AtomicBoolean`，当前没有多节点 leader election。
- Platform 已引入 Actuator，但仍缺 Kairo 业务 readiness、低基数指标、统一 build identity 和可用支持包。
- 当前 Maven/Docker/运行时仍大量使用 `0.1.0-SNAPSHOT`，尚未形成一致的 `1.7.0` 发布物。
- 当前兼容 CI 存在 `continue-on-error`、失败探测和占位 job，不能作为正式支持证据。

任何实现若与以上事实冲突，应提交设计偏差，不得通过增加新模块、旁路状态或扩大基础设施来绕过。

## 3. V1.7 运行与支持边界

### 3.1 正式运行拓扑

V1.7.0 正式支持的控制面拓扑固定为：

```text
Browser / CLI / SDK / MCP
            |
     Platform Web
            |
  单个 Platform API + Scheduler 进程
        |                 |
  PostgreSQL 16       Redis 7（仅 fencing，按配置可选）
        |
Agent 主动注册、心跳、轮询命令
        |
目标 JVM
```

边界说明：

- PostgreSQL 是权威状态；Redis 丢失不得造成规则、发布和审计数据丢失。
- Redis 启用时不可用可以阻止需要新 fencing token 的写操作，但不应误杀 Platform liveness。
- 正式支持单个活跃 Platform 进程及其重启恢复；不承诺两个 Platform scheduler 同时主动运行。
- Agent 只能主动连接 Platform；Platform 不直连 Agent 暴露地址。
- Platform 不可用时，目标业务方法和已加载 Agent 继续运行；本地 `kairo-ops` 仍可执行紧急 disable/reset。

### 3.2 候选支持矩阵

M3 必须用真实 CI 把候选矩阵冻结为最终矩阵。在证据形成前，候选声明如下：

| 组件/维度 | 候选正式支持 | 明确不承诺 |
|---|---|---|
| Platform | Linux x86_64，JDK 21，PostgreSQL 16，Redis 7 | Windows Platform、多节点 HA、多区域 |
| Platform Web | Linux 容器，Node 20 构建产物 | 浏览器插件或桌面客户端 |
| Agent 目标 JVM | JDK 17、21 | JDK 8、11 完整 Agent Core |
| 加载方式 | premain、agentmain、外部 attach | 绕过目标 OS/JDK attach 限制 |
| 目标形态 | 普通 Java、Spring Boot 3、父子同名 ClassLoader、JDK Proxy、CGLIB、Byte Buddy、Lambda、bridge/synthetic | 未经矩阵验证的框架/语言自动承诺 |
| OS | Linux x86_64 为主支持；macOS arm64/JDK 21 在真实 CI 通过后可正式支持 | Windows 在 V1.7.0 默认 `EXPERIMENTAL` |
| 数据库 | PostgreSQL 16 | MySQL、Oracle、SQL Server 生产运行 |

`kairo-agent-bootstrap` 和 `kairo-bootstrap-api` 是 Java 8 class file，不代表完整 Agent 支持 JDK 8；
`kairo-agent-core-modern` 和其他核心模块以 Java 17 为基线，Platform 明确以 Java 21 编译。V1.7 不为 JDK 8/11
再建设一套 Spring Boot 2/旧 Groovy/旧 Byte Buddy 核心发行物。

### 3.3 明确不做

V1.7 不允许夹带以下工作：

- 新增强位置、新规则链语义、新脚本档位或扩大脚本权限；
- 新 Web 业务页面、新 AI/MCP 工具、新公开产品资源族；
- 录制回放、数据集、Kafka、MinIO、审批流或实验编排；
- Kubernetes、Helm、Service Mesh、OIDC/SSO、Vault、云 KMS；
- 多 Platform 主动—主动、分布式 leader election、多区域容灾；
- JDK 8/11 legacy core、Spring Boot 2 专用发行线、Windows 全面适配；
- 为清理代码而进行全仓重命名、模块拆分、框架升级或控制器重写；
- 以 `RESET_ALL` 代替精确恢复，或以修改测试断言代替修复产品缺陷；
- 只为“看起来像 LTS”而增加没有使用者、没有验收场景的基础设施。

出现上述需求时，记录到 V2 或独立兼容发行线，不阻塞 V1.7。

## 4. 不可违反的可靠性语义

### 4.1 PostgreSQL 权威性

- 正式规则、版本、desired state、发布计划、实例执行、命令状态、卸载状态和审计以 PostgreSQL 为准。
- 内存对象只能是缓存、等待器或临时敏感载荷，不能成为正式规则的唯一事实来源。
- 已提交的终态不能因进程重启回退；派生状态必须可从权威表重新计算。

### 4.2 命令语义

Agent 命令分为两类，分类是固定设计，不得由实现者随意改变。

#### DURABLE：允许安全恢复和重试

- `APPLY_RULE`
- `APPLY_CHAIN`
- `DISABLE_ALL`
- `ENABLE_ALL`
- `RESET_CLASS`
- `RESET_ALL`
- `STOP_AGENT`
- `REFRESH_RUNTIME_STATE`

DURABLE 命令的 payload 必须完整持久化；执行必须依赖 idempotency key、dispatch epoch、期望 revision/hash
中的适用字段。发生重试时，旧 owner 不能完成新 owner 的命令轮次。

#### TRANSIENT：进程重启后明确失败，不自动重放

- `START_RECORDING`、`STOP_RECORDING`
- `DISCOVER_TARGETS`、`LIST_LOADERS`、`LIST_CALL_SITES`、`RESOLVE_TARGET`
- 所有 `BYTECODE_*`
- 所有 `SCRIPT_*`

这类命令可能依赖内存中的脚本正文、class bytes、等待器或 JVM 瞬时观察。Platform 重启、等待器丢失或
deadline 到期后，命令进入机器可读失败态，由调用者重新发起。禁止为了“可恢复”而把脚本正文、敏感字节码或
大对象无边界写入数据库。

### 4.3 命令状态机

固定状态机：

```text
PENDING
  -> DISPATCHED(attempts=N, leaseExpiresAt=T)
      -> ACKED
      -> FAILED
      -> lease 过期且 attempts < maxAttempts -> DISPATCHED(attempts=N+1)
      -> lease 过期且 attempts >= maxAttempts -> FAILED(MAX_ATTEMPTS_EXHAUSTED)
```

规则：

- 同一轮 dispatch 只能有一个成功 claim。
- live lease 不可被抢占。
- Agent ACK 必须回显本轮 `attempts` 作为 `expectedAttempts`。
- V1.6 Agent 缺少 `expectedAttempts` 时，只允许 `attempts == 1` 的首次派发 ACK；重新派发后缺少 epoch 必须拒绝。
- ACKED/FAILED 是不可变终态；重复 ACK 返回同一结果或明确 conflict，不能再次推进 rollout/unload。
- `attempts >= maxAttempts` 的过期命令必须被终结，不能永久留在不可见的 DISPATCHED 状态。

### 4.4 desired/actual 收敛

- Platform desired state 只包含正式、仍有效的规则版本；临时试运行和已过期 TTL 规则不进入重启恢复集合。
- Agent actual state 必须来自真实运行时快照，不得由“命令曾 ACK”推测。
- `IN_SYNC`：revision 和 canonical hash 一致，不执行命令。
- `MISSING/BEHIND`：自动补发当前 desired state。
- `AHEAD/DIVERGED`：标记 degraded 并停止自动覆盖；用户通过既有重新发布或卸载操作处理。
- transformation hash 漂移必须遵循 V1.4/V1.5 的其他 Agent 共存和 `TARGET_DRIFTED` 语义，不能恢复最初 class bytes。

### 4.5 Agent 离线不等于已卸载

- 心跳租约过期只能把 Agent/实例标为离线或运行态未知。
- 不得仅因 Agent 不可达就把 operation plan 写成 `UNLOADED` 或把 runtime status 写成 `REMOVED`。
- 若同一 JVM 进程恢复连接，应先读取 actual state，再继续待处理卸载或对账。
- 若 `processStartId` 变化，说明是新 JVM 生命周期；旧内存增强自然消失，但正式 desired state 需要重新应用。
- 实例被长期清理时可以把执行历史标为 `ABANDONED`，但必须保留“未确认卸载”的历史事实。

### 4.6 失败边界

- 单个 JVM 内规则链应用继续使用现有原子快照和转换回滚。
- 多 JVM 发布不伪装成分布式事务；每个实例分别记录成功、失败、离线待补偿。
- 外部不可事务副作用维持 at-least-once 边界，并通过 idempotency/fencing 降低重复影响。
- Platform 不可用不能把异常传播到目标业务方法；Agent 运行时继续 fail-open。

## 5. 证据模型

### 5.1 门禁级别

同一 requirement 必须分别记录三个门禁，禁止用一次本地测试宣称整个 requirement 已完成：

```text
PR       代码与短程回归门禁
RC       固定 CI/受控环境候选版门禁
RELEASE  最终发布、长稳、签名和人工批准门禁
```

每个门禁状态只能是：

- `PASSED`
- `FAILED`
- `SKIPPED`
- `NOT_RUN`
- `EXPERIMENTAL`

`SKIPPED/NOT_RUN/EXPERIMENTAL` 不能成为正式支持或最终发布证据。

### 5.2 acceptance manifest v2

`v1.7-acceptance-manifest.json` 在首次记录 M1 证据前升级为 schema `2.0`。最低结构：

```json
{
  "schemaVersion": "2.0",
  "release": "V1.7.0",
  "buildId": "<git commit>",
  "requirements": [
    {
      "id": "V17-RECOVERY",
      "gates": {
        "PR": {
          "status": "PASSED",
          "environment": {},
          "commands": [],
          "reports": [],
          "startedAt": "...",
          "endedAt": "...",
          "limitations": []
        },
        "RC": { "status": "NOT_RUN" },
        "RELEASE": { "status": "NOT_RUN" }
      }
    }
  ]
}
```

M0 已存在的 schema 1.0 证据保留其历史事实；迁移到 v2 时只重排结构，不得把尚未执行的 RC/RELEASE
门禁补写为 `PASSED`。

### 5.3 固定证据文件

| 文件 | 责任里程碑 | 含义 |
|---|---|---|
| `v1.7-acceptance-manifest.json` | M0～M6 | 所有 requirement 的门禁索引 |
| `target/v1.7/recovery-result.json` | M1 | 恢复场景逐项结果 |
| `target/v1.7/benchmark-result.json` | M2 | 性能原始样本和比较结果 |
| `target/v1.7/soak-result.json` | M2/M6 | 长稳参数、时间序列摘要、退出原因 |
| `target/v1.7/compatibility-result.json` | M3 | 精确矩阵行及真实执行结果 |
| `target/v1.7/ops-result.json` | M4 | health/metrics/支持包验证结果 |
| `target/v1.7/release-manifest.json` | M5/M6 | 最终 artifact、hash、SBOM、签名和 provenance |

`target/` 下的文件是 CI 构建产物，不提交大型原始时间序列；仓库只提交 schema、生成器、预算和必要小型基线。

## 6. 实施顺序总览

| 里程碑 | 目标 | 是否写产品代码 | 进入条件 | 完成后状态 |
|---|---|---:|---|---|
| M0 | 冻结 V1.6 契约 | 是 | V1.6 已验收 | 已完成 |
| M1 | 恢复与状态一致性 | 是 | M0 通过 | 可恢复 |
| M2 | 性能、循环和泄漏验证 | 少量，主要为 harness/缺陷修复 | M1 通过 | 可测量 |
| M3 | 真实支持矩阵 | 少量，主要为 fixture/CI/兼容缺陷 | M2 短程门禁通过 | 可声明支持 |
| M4 | 最低限度可运维性 | 是 | M1 状态语义冻结 | 可诊断恢复 |
| M5 | 版本与发布供应链 | 是，限构建/元数据 | M0～M4 PR 门禁通过 | Engineering Complete |
| M6 | 最终 LTS 认证 | 原则上否，只修阻断缺陷 | M5 完成 | LTS Certified |

后一个里程碑不能掩盖前一个的失败。M2/M3/M4 可以在 M1 核心语义冻结后准备 fixture，但不得提前宣布通过。

## 7. M0：契约冻结（已完成）

### 7.1 已交付

- 规范化 OpenAPI V1 快照与兼容比较；
- 错误码、配置目录和 Agent protocol/capability 快照；
- V1～V41 迁移 hash、历史来源和代表性升级 fixture；
- V1.6 Platform/Agent 交叉 wire fixture；
- 机器可读验收 manifest v1；
- Maven reactor、Web lint/typecheck/test/build 回归证据。

### 7.2 证据 commit

- `cfb6ec70a499a6b8d689272f996aa7291e8f8c80`：冻结兼容基线；
- `e43fbd1`：记录 M0 验收证据。

### 7.3 后续限制

- 不修改、删除、重排 V1～V41 已发布迁移。
- V1 API/错误码/配置/协议只允许向后兼容追加。
- M1 增加的 `expectedAttempts` 属于可选 additive 字段；旧客户端行为必须由兼容测试明确保护。

## 8. M1：恢复与状态一致性

M1 只能按 M1-A～M1-F 顺序执行。一个 Claude 会话只执行一个工作包，不允许一次领取整个 M1。

### 8.1 M1-A：命令 lease、epoch 与终态安全

#### 目标

建立可证明的 Agent command claim/ack 状态机，消除重复 claim、live lease 抢占、旧 owner ACK、终态覆盖和
最大尝试耗尽后永久卡住的问题。

#### 允许修改

- `kairo-platform-server/.../command/AgentCommandService.java`
- `kairo-platform-server/.../persistence/mapper/AgentCommandMapper.java`
- `kairo-platform-server/src/main/resources/mapper/AgentCommandMapper.xml`
- `kairo-agent-server/.../PlatformCommandPoller.java`
- 必要的共享可选协议常量/DTO；
- M1-A 专项测试和小型 fixture。

#### 禁止修改

- 历史 Flyway SQL；
- rollout/unload 业务语义；
- Web、CLI、MCP；
- 规则链实现、脚本引擎、字节码转换实现；
- 通过提高 sleep/timeout 掩盖竞态。

#### 必须实现

1. `dispatchCommand` 使用原子条件更新：PENDING 可 claim；DISPATCHED 只有 lease 已过期才能重新 claim。
2. 每次成功 dispatch 后 `attempts + 1`，响应中返回该值。
3. Agent 所有 ACK 路径都回传 `expectedAttempts`，包括成功、capability failure 和异常失败。
4. ACK SQL 同时校验 `status=DISPATCHED` 与 dispatch epoch。
5. Legacy V1.6 ACK 未携带 epoch 时只允许 `attempts=1`；`attempts>1` 必须返回 `AGENT_COMMAND_STATE_CONFLICT`。
6. 重复/延迟 ACK 不再次执行 rollout/unload/审计推进。
7. lease 过期且 `attempts>=maxAttempts` 的命令原子终结为 FAILED，错误码固定为
   `AGENT_COMMAND_MAX_ATTEMPTS_EXHAUSTED`。
8. 全部时间由可注入 `Clock` 控制；并发测试使用 barrier/latch，不使用长 sleep。

#### 必须新增或等价测试

- `AgentCommandLeaseIntegrationTest`
- `AgentCommandAckFencingIntegrationTest`
- `AgentCommandPollerEpochTest`

覆盖：

- 两个 claim 并发只有一个成功；
- live lease 不可抢；
- expired lease 只有一个接管者；
- 当前 epoch ACK 成功；
- stale epoch、未来 epoch 和缺失 epoch 的重新派发 ACK 被拒绝；
- legacy 首次派发 ACK 仍成功；
- ACKED/FAILED 终态不变；
- maxAttempts 耗尽后进入明确 FAILED。

#### 验收命令

```bash
mvn -B -ntp -pl kairo-platform-server,kairo-agent-server -am \
  test -Dtest='AgentCommand*Test,PlatformCommandPollerEpochTest' \
  -Dsurefire.failIfNoSpecifiedTests=false
mvn -B -ntp -pl kairo-platform-server,kairo-agent-server -am test
git diff --check
```

#### 完成条件

专项测试和两个模块全量测试通过；Agent 实际回传 epoch；没有未终结的 maxAttempts 命令；没有改动 M1-A
允许范围以外的产品语义。

### 8.2 M1-B：Platform 重启与命令分类

#### 目标

证明同一持久化数据库上的 Platform 重启不会丢正式状态或重复推进终态，并让 TRANSIENT 请求在等待器丢失后
明确失败。

#### 允许修改

- Platform command/maintenance/startup recovery 服务；
- command mapper/XML；
- application test 配置；
- M1-B 集成测试；
- 必要时追加新的 Flyway 迁移，但不得修改 V1～V41。

#### 必须实现

1. 测试以同一个 file-backed H2 数据库关闭并重新创建 Spring context；不能用同一 context 模拟重启。
2. 重启后 PENDING DURABLE 命令可继续 claim。
3. 重启后过期 DISPATCHED DURABLE 命令可按 M1-A 重新派发。
4. ACKED/FAILED 命令不再返回给 Agent，不重复推进 rollout/unload。
5. orphan TRANSIENT PENDING/DISPATCHED 命令进入 FAILED，错误码固定为
   `TRANSIENT_COMMAND_CONTEXT_LOST`，保留审计和最小诊断。
6. 脚本正文、class bytes、Authorization/Token 不因恢复改造写入数据库或日志。
7. 恢复动作幂等：连续启动两次不会重复改变已恢复行。

#### 必须新增或等价测试

- `PlatformRestartRecoveryIntegrationTest`
- `TransientCommandRestartIntegrationTest`
- `CompletedCommandNoReplayIntegrationTest`

#### 验收命令

```bash
mvn -B -ntp -pl kairo-platform-server -am \
  test -Dtest='PlatformRestartRecoveryIntegrationTest,TransientCommandRestartIntegrationTest,CompletedCommandNoReplayIntegrationTest' \
  -Dsurefire.failIfNoSpecifiedTests=false
mvn -B -ntp -pl kairo-platform-server -am test
git diff --check
```

### 8.3 M1-C：真实 Agent runtime snapshot

#### 目标

把 `REFRESH_RUNTIME_STATE` 从占位响应改成真实运行时事实来源，为 JVM 重启和 desired/actual 对账提供输入。

#### 允许修改

- `kairo-agent-core` 只读快照方法；
- `kairo-agent-server/PlatformCommandPoller`；
- `kairo-api` 中 additive snapshot DTO；
- Platform actual-state 持久化服务/mapper；
- 必要的新迁移和测试。

#### snapshot 最低字段

```text
protocolVersion
agentId
processStartId
observedAt
agentVersion
disabled
chains[]:
  chainId
  target identity（className/loaderId/method/descriptor/location/callSite）
  appliedRevision
  canonicalHash
  transformationRevision
  transformationHash
  desiredState
  ruleIds
  degradedReason
rules[]:
  ruleId
  ruleVersion
  enabled
  expireAt
degradedClasses[]
```

#### 必须实现

1. snapshot 只读取一致的 Agent 内存快照，不触发增强、卸载、编译或反编译。
2. 响应有最大规则数、最大字节数和稳定排序；超限返回结构化截断信息，不能 OOM。
3. Platform 验证 schema/agent/processStartId 后再写 actual state；旧 processStartId 的迟到 snapshot 被拒绝。
4. snapshot 不包含脚本正文、Token、原始 class bytes 或无限事件列表。
5. `REFRESH_RUNTIME_STATE` 是 DURABLE 且幂等的只读命令。

#### 必须新增或等价测试

- `RuntimeStateSnapshotTest`
- `PlatformRuntimeStateCommandTest`
- `RuntimeStateSnapshotPersistenceIntegrationTest`
- 一个真实 AgentRuntime 测试：应用规则后 snapshot 有规则，卸载后消失。

#### 验收命令

```bash
mvn -B -ntp -pl kairo-agent-core,kairo-agent-server,kairo-platform-server -am \
  test -Dtest='RuntimeStateSnapshotTest,PlatformRuntimeStateCommandTest,RuntimeStateSnapshotPersistenceIntegrationTest' \
  -Dsurefire.failIfNoSpecifiedTests=false
mvn -B -ntp -pl kairo-agent-core,kairo-agent-server,kairo-platform-server -am test
git diff --check
```

#### 完成条件

真实 AgentRuntime 应用/卸载前后的 snapshot 与实际规则一致；Platform 拒绝旧 processStartId；响应上限和脱敏测试通过；
未把 snapshot 实现成新的增强或持久化大对象入口。

### 8.4 M1-D：注册、重连和 JVM 重启对账

#### 目标

Agent 注册、重新连接或 `processStartId` 变化后，自动比较正式 desired state 与真实 actual snapshot，并在安全边界内
收敛。

#### 允许修改

- Agent 注册/心跳后的 Platform 协调逻辑；
- RuleChainStateService 及专用 reconciliation service/scheduler；
- rollout/command enqueue 的复用点；
- 相关 mapper、新迁移和测试。

#### 禁止修改

- 新增公开“强制覆盖漂移”API；
- 遇到所有差异直接 `RESET_ALL`；
- 把临时 script session 写成正式 desired state；
- 通过删除 actual state 掩盖不一致。

#### 必须实现

1. Agent 注册成功后请求一次 runtime snapshot；并发注册不能产生命令风暴。
2. 同一 `processStartId` 重连：保留 actual 历史，读取新 snapshot 后继续未完成补偿。
3. 新 `processStartId`：旧 actual 标记为历史/失效，读取新 JVM 空状态。
4. 正式 desired `ACTIVE` 且 actual MISSING/BEHIND：只补发当前有效版本。
5. desired 已 EMPTY/REMOVED 且 actual 仍存在：发起精确卸载。
6. AHEAD/DIVERGED/TARGET_DRIFTED：标记 DEGRADED，不自动破坏性覆盖。
7. TTL 已到期、试运行、未 promote 的 session 不进入恢复集合。
8. 同一 reconciliation key 在未完成时不重复入队；完成后再次运行是 no-op。

#### 必须新增或等价测试

- `AgentReconnectReconciliationIntegrationTest`
- `JvmRestartReapplyIntegrationTest`
- `ExpiredTrialDoesNotReviveIntegrationTest`
- `DivergedStateFailClosedIntegrationTest`
- 真实 JVM 行为测试：重启 AgentRuntime 后正式规则重新应用，TTL 规则不复活。

#### 验收命令

```bash
mvn -B -ntp -pl kairo-platform-server,kairo-agent-server,kairo-integration-tests -am \
  test -Dtest='AgentReconnectReconciliationIntegrationTest,JvmRestartReapplyIntegrationTest,ExpiredTrialDoesNotReviveIntegrationTest,DivergedStateFailClosedIntegrationTest' \
  -Dsurefire.failIfNoSpecifiedTests=false
mvn -B -ntp -pl kairo-platform-server,kairo-agent-server,kairo-integration-tests -am test
git diff --check
```

#### 完成条件

同进程重连、新 JVM、missing/behind、diverged 和 TTL 五类状态都有确定行为；重复 reconciliation 是 no-op；
真实 JVM 方法行为与最终 desired state 一致。

### 8.5 M1-E：离线卸载、多目标失败和补偿

#### 目标

纠正“Agent 离线即已卸载”的错误假设，让发布/卸载的每个实例状态真实可解释并可补偿。

#### 必须实现

1. 删除或重构 `markPlansUnloadedWhenAgentsGone` 的假成功语义。
2. Agent 离线时，已增强目标标为 `UNKNOWN/OFFLINE_PENDING` 或等价非终态，不能标 `REMOVED`。
3. 离线期间提交卸载：创建持久化待补偿记录；Agent 恢复后读取 actual，再执行精确卸载。
4. 新 JVM processStartId 且 actual 空：可确认旧 JVM 增强已消失，再按 desired 判断重新应用或完成卸载。
5. 多目标发布/卸载分别记录成功、失败、离线待处理；聚合状态不覆盖实例事实。
6. 重试使用原 target snapshot 和 idempotency key，不重新扩大目标范围。
7. 达到最大尝试后进入可诊断 FAILED，不永久 RUNNING/UNLOADING。

#### 必须新增或等价测试

- `OfflineAgentUnloadCompensationIntegrationTest`
- `AgentGoneIsNotUnloadedIntegrationTest`
- `MultiTargetPartialFailureIntegrationTest`
- `UnloadRetryIdempotencyIntegrationTest`
- 真实 JVM 测试：断开 Platform、保持 JVM 存活、恢复连接后完成卸载并恢复方法行为。

#### 验收命令

```bash
mvn -B -ntp -pl kairo-platform-server,kairo-agent-server,kairo-integration-tests -am \
  test -Dtest='OfflineAgentUnloadCompensationIntegrationTest,AgentGoneIsNotUnloadedIntegrationTest,MultiTargetPartialFailureIntegrationTest,UnloadRetryIdempotencyIntegrationTest' \
  -Dsurefire.failIfNoSpecifiedTests=false
mvn -B -ntp -pl kairo-platform-server,kairo-agent-server,kairo-integration-tests -am test
git diff --check
```

#### 完成条件

离线不再产生假 UNLOADED；待补偿卸载在重连后完成；多目标事实不被聚合状态覆盖；真实 JVM 恢复原方法行为。

### 8.6 M1-F：依赖故障、紧急停止和回退保护

#### 目标

完成单 Platform 支持边界内的数据库/Redis 故障行为、Platform 不可用时的本地紧急恢复，以及应用回退保护。

#### 必须实现或证明

1. PostgreSQL 短时不可用：readiness DOWN、liveness 保持 UP；连接恢复后未完成 DURABLE 命令继续处理。
2. Redis 未启用：完全不影响 readiness；Redis 启用且不可用：需要 fencing 的写入失败为结构化错误，
   readiness DOWN，权威 DB 状态不丢失。
3. Platform 不可用时，`kairo-ops disable-all/reset-class/reset-all` 仍可通过 loopback Agent API 工作并写本地审计。
4. Platform 恢复后 actual snapshot 反映紧急操作结果，reconciliation 不盲目撤销人工紧急恢复。
5. 应用回退前检查当前 Flyway schema 是否兼容；不兼容时拒绝启动并指向升级前备份恢复步骤。
6. 不实现自动 down migration。

#### 必须新增或等价测试

- `DependencyHealthRecoveryIntegrationTest`
- `RedisFencingFailureIntegrationTest`
- `EmergencyOpsWithoutPlatformIntegrationTest`
- `PostEmergencyReconciliationIntegrationTest`
- `ApplicationRollbackGuardIntegrationTest`

#### 验收命令

```bash
mvn -B -ntp -pl kairo-platform-server,kairo-ops,kairo-integration-tests -am \
  test -Dtest='DependencyHealthRecoveryIntegrationTest,RedisFencingFailureIntegrationTest,EmergencyOpsWithoutPlatformIntegrationTest,PostEmergencyReconciliationIntegrationTest,ApplicationRollbackGuardIntegrationTest' \
  -Dsurefire.failIfNoSpecifiedTests=false
mvn -B -ntp -pl kairo-platform-server,kairo-ops,kairo-integration-tests -am test
git diff --check
```

#### 完成条件

DB/Redis 故障和恢复、Platform 不可用时本地 reset、恢复后不盲目重放、应用回退保护全部有自动化证据；
没有引入自动 down migration 或新的高可用基础设施。

### 8.7 M1 总体验收

M1 完成必须同时满足：

- M1-A～M1-F 专项测试全部通过；
- `mvn -B -ntp test` 全 reactor 通过；
- Web 的 lint/typecheck/test/build 无回归；
- 至少一个 Platform → Agent → 真实 JVM → ACK → 重启/重连 → 对账 → 卸载闭环；
- `target/v1.7/recovery-result.json` 覆盖每个场景，未执行项保持 `NOT_RUN`；
- `V17-RECOVERY.PR` 才能标记 `PASSED`，RC/RELEASE 仍保持 `NOT_RUN`。

## 9. M2：性能、状态循环和资源泄漏

M2 的任务是测量现有系统并修复被证据证明的缺陷，不是重新设计 Agent。

### 9.1 M2-A：基线 harness 与预算

#### 允许修改

- 新增独立 benchmark/soak harness，可放在 `kairo-integration-tests` 的专用 source set 或新增仅测试模块；
- 必要的只读计数器和测试钩子；
- 被真实基准证明的局部性能缺陷。

#### 禁止修改

- 仅为跑分改变规则语义或安全默认值；
- 在普通 JUnit 中用单次 `System.nanoTime()` 作为跨机器硬 SLO；
- 引入生产必需的 profiler/APM；
- 在业务热路径增加网络或数据库调用。

#### 测量场景

- 无 Agent 基线；
- Agent 已加载但目标未增强；
- 目标已增强但规则未命中；
- BEFORE/RETURN/THROWS 命中；
- Groovy no-op/参数读取/返回替换；
- 1、5、20 条规则链；
- 大量已加载类和规则的查询；
- 事件缓冲区满载、丢弃和背压。

#### 采样规则

- 固定 runner/JDK/JVM 参数；
- 预热、至少 5 个 fork 或等价隔离进程、每个场景保存原始样本；
- 输出 median/P95/P99、误差和环境；
- V1.6 与 V1.7 在同一 runner 依次执行；
- 初始相对预算：关键未命中/命中场景的 median/P95 不得比 V1.6 退化超过 20%；超过时必须分析，
  不能简单提高阈值；
- 首次有效基线写入小型 `v1.7-performance-budget.json`，后续调整必须经过文档评审。

### 9.2 M2-B：10,000 次真实状态循环

每个 cycle 必须真实执行：

1. 增强目标类；
2. 调用目标方法并验证行为；
3. 更新或局部卸载规则；
4. 再次调用验证；
5. 完全卸载；
6. 验证方法行为和规范化字节码 hash 回到正确基线。

覆盖普通方法、构造器、调用点、规则链、父子 ClassLoader 同名类以及一次并发冲突场景。禁止 sleep-only、
只修改内存模型或只验证 registry 数量。

### 9.3 M2-C：ClassLoader/Groovy 泄漏

覆盖：

- 可卸载业务 ClassLoader；
- Groovy 脚本 ClassLoader 和 compile cache；
- JDK Proxy、CGLIB、Byte Buddy 生成类；
- Lambda、bridge/synthetic；
- 字节码 snapshot、journal、method cache、rule registry 和统计缓存。

短程 PR 门禁：100～500 cycle。RC 门禁：10,000 cycle 和 2 小时 soak。观测：heap、Metaspace、线程、
live ClassLoader weak reference、已加载类和文件描述符（平台支持时）。

初始判定预算：

- 所有显式创建的可卸载 ClassLoader 在最终强引用分析后最多残留 2 个；
- 运行线程数相对稳定窗口增长不超过 2；
- 文件描述符相对稳定窗口增长不超过 5；
- 最后一个稳定窗口的 full-GC 后 heap 不高于首个稳定窗口 15%；
- Metaspace 不高于首个稳定窗口 10%；
- snapshot/cache/rule 计数在完全卸载后回到预算内。

如果平台、GC 或测试噪声证明某数字不适用，必须先提交包含原始样本的预算调整，不允许测试自行放宽。

### 9.4 M2-D：长稳 harness

- 每分钟采集时间序列摘要；
- 持续执行真实调用，每 5 分钟执行一批增强/更新/卸载，每 30 分钟执行 Agent/Platform 断连恢复；
- RC 固定运行 2 小时；最终 RELEASE 固定运行 7 天；
- 失败条件：进程异常退出、OOME、业务异常泄漏、永久状态漂移超过 5 分钟、资源持续超过预算、无法完成精确卸载；
- 输出 `soak-result.json`，包含 build ID、JDK/JVM 参数、周期数、失败时间、最后状态和原始时间序列 URI。

### 9.5 M2 固定脚本接口

M2 必须交付以下仓库脚本接口，后续 CI 和 M6 只能调用这些接口，不再各写一套临时命令：

```bash
# V1.6 与当前 commit 同机对比，PR 短程
./scripts/v1.7/run-performance.sh \
  --mode pr --baseline V1.6.0 --candidate HEAD \
  --output target/v1.7

# 真实增强/调用/局部卸载/完全卸载循环
./scripts/v1.7/run-state-cycle.sh \
  --cycles 500 --output target/v1.7

# ClassLoader、Groovy、proxy/Lambda 泄漏短程
./scripts/v1.7/run-leak-check.sh \
  --cycles 500 --output target/v1.7

# RC 使用 --duration PT2H；M6 使用 --duration P7D
./scripts/v1.7/run-soak.sh \
  --duration PT2H --output target/v1.7

# 只验证 schema、build ID、预算和退出状态，不重新跑测试
./scripts/v1.7/verify-performance-evidence.sh target/v1.7
```

脚本必须：非零退出表示门禁失败；支持 `--help`；记录完整命令、环境和 build ID；不自动修改预算或验收 manifest；
不下载未锁定版本的工具。脚本内部可以调用 Maven/Java harness，但不能以 shell sleep 模拟增强循环。

### 9.6 M2 完成条件

- `V17-PERF.PR` 和短程 `V17-SOAK.PR` 通过；
- 10,000 cycle、2 小时 soak 属于 RC 门禁；
- 7 天 soak 只在 M6 标记 RELEASE；
- 性能或泄漏失败若暴露产品缺陷，使用 `bugfix/v1.7-<scope>` 最小修复，不进行无证据重构。

## 10. M3：真实兼容矩阵

### 10.1 固定矩阵行

以下行是 V1.7.0 候选正式支持的最低真实测试，不再用模糊“风险覆盖”替代：

| ID | Runner | 目标 JVM | 加载 | Fixture | 必须行为 |
|---|---|---:|---|---|---|
| C01 | Linux x86_64 | 17 | premain | plain Java | 增强、调用、更新、卸载 |
| C02 | Linux x86_64 | 17 | external attach/agentmain | plain Java | attach、增强、卸载、shutdown |
| C03 | Linux x86_64 | 21 | premain | Spring Boot 3 executable jar | 注册、发布、调用、卸载 |
| C04 | Linux x86_64 | 21 | external attach | Spring Boot 3 executable jar | attach、发布、卸载 |
| C05 | Linux x86_64 | 21 | premain | parent/child same-name loaders | 只增强指定 loader |
| C06 | Linux x86_64 | 21 | premain | JDK Proxy/CGLIB/Byte Buddy | 目标解析与精确卸载 |
| C07 | Linux x86_64 | 17、21 | premain | Lambda/bridge/synthetic | 发现、策略、实际行为 |
| C08 | Linux x86_64 | 21 | premain | redefine/retransform/hot update | 成功对账或明确 TARGET_DRIFTED |
| C09 | macOS arm64 | 21 | agentmain | plain Java | 真实 attach、增强、卸载 |
| C10 | Linux x86_64 | 21 | premain | 与一个受控 Byte Buddy Agent 共存 | Kairo 卸载不破坏对方变换 |

C09 若没有真实 macOS CI，最终状态为 `EXPERIMENTAL`，且 macOS 不进入正式支持。C10 只验证一个仓库内固定
共存 fixture，不扩展为所有第三方 APM 的兼容承诺。

### 10.2 明确非正式行

| 组合 | V1.7.0 状态 |
|---|---|
| JDK 8/11 目标 JVM | `NOT_SUPPORTED` |
| Windows | `EXPERIMENTAL`，除非补齐与 C01～C04 等价真实 CI |
| Spring Boot 2 | `EXPERIMENTAL` |
| Kotlin 默认参数/协程 | `EXPERIMENTAL`，不阻断 V1.7 |
| Tomcat 独立部署 | `EXPERIMENTAL`，除非增加真实 fixture |
| 多 Platform 节点 | `NOT_SUPPORTED` |

### 10.3 CI 要求

- 删除承诺矩阵行上的 `continue-on-error`；
- 每个 job 上传相同 schema 的行级 JSON；
- 汇总 job 生成唯一 `compatibility-result.json`；
- 承诺行 `FAILED/SKIPPED/NOT_RUN` 时汇总 job 失败；
- 只编译、只探测版本、模拟 OS、打印 warning 不算执行；
- Agent 目标 JVM 必须是独立真实进程，不能只在测试 JVM 内调用类库。

固定实现接口：

```bash
# 本地/CI 执行单行；scenario 必须来自本文 C01～C10
./scripts/v1.7/run-compatibility.sh \
  --scenario C01 --output target/v1.7/compatibility-rows/C01.json

# 聚合所有行并按候选正式/实验范围决定退出码
./scripts/v1.7/aggregate-compatibility.sh \
  --input target/v1.7/compatibility-rows \
  --output target/v1.7/compatibility-result.json

# 验证 JSON 与 docs/compatibility/v1.7.md、release manifest 的结论一致
./scripts/v1.7/verify-compatibility.sh target/v1.7/compatibility-result.json
```

权威 workflow 保持 `.github/workflows/compatibility-matrix.yml`；不得另建第二个互相矛盾的正式矩阵 workflow。

### 10.4 M3 完成条件

- C01～C08、C10 全部 PASSED；
- C09 按真实证据决定 PASSED 或 EXPERIMENTAL；
- 生成 `docs/compatibility/v1.7.md`，内容由同一 JSON 生成，不手写不同结论；
- `V17-COMPAT.RC` 只有在最终候选 commit 上运行后才能 PASSED。

## 11. M4：最低限度可运维性

### 11.1 health 语义

必须提供：

- `/actuator/health/liveness`：只反映进程是否能继续服务；DB/Redis/Agent 离线不能让 liveness DOWN。
- `/actuator/health/readiness`：DB、Flyway validate 和配置为必需的 Redis 正常时 UP。
- `/actuator/info`：version、git commit、build time、Java target、contract baseline。

测试必须真实断开并恢复 DB/Redis，验证 HTTP status、body 和恢复，不允许 mock 一个固定 DOWN indicator 代替。

### 11.2 固定业务指标

以下名称作为 V1.7 运维契约；实现使用 Micrometer，标签只能来自本文列出的有限集合：

| 指标 | 类型 | 允许标签 |
|---|---|---|
| `kairo_agent_online` | gauge | `status` |
| `kairo_agent_command_backlog` | gauge | `status`, `command_type` |
| `kairo_agent_command_total` | counter | `command_type`, `result` |
| `kairo_operation_total` | counter | `operation_type`, `result` |
| `kairo_operation_duration_seconds` | timer | `operation_type`, `result` |
| `kairo_runtime_rule_targets` | gauge | `state` |
| `kairo_reconcile_total` | counter | `result` |
| `kairo_rollback_total` | counter | `result` |
| `kairo_ttl_cleanup_total` | counter | `result` |
| `kairo_platform_build_info` | gauge=1 | `version`, `commit` |

禁止使用 ruleId、agentId、instanceId、className、methodName、traceId、用户名等无界 label。需要定位具体对象时使用
结构化日志和现有资源查询。

### 11.3 支持包

不新增复杂 Web 页面。复用现有 CLI 边界：

- `kairo-cli diagnose --output <zip>` 收集 Platform health/info、指标名和必要值、版本、最近操作摘要；
- `kairo-ops support-bundle --output <zip>` 收集本地 Agent status、有限事件和指标；
- 默认不收集脚本正文、Token、Authorization、环境变量值、原始 class bytes、完整 heap dump；
- 配置只保存键、来源类型和脱敏值；
- 单个 zip 默认上限 20 MiB，默认超时 30 秒；
- 测试植入 canary secret，zip 文件名、entry 名和解压内容均不得出现 secret。

实现若发现现有 CLI 无法复用，可以增加内部只读 endpoint，但不得创建新的业务资源族或浏览器功能。

### 11.4 runbook

最低文档：

- Platform 启动失败/Flyway 失败；
- PostgreSQL/Redis 不可用；
- Agent 离线或命令积压；
- 发布卡住、部分失败和状态漂移；
- 卸载失败和离线待补偿；
- TTL 未清理；
- heap/Metaspace/ClassLoader 持续增长；
- Platform 不可用时使用 `kairo-ops` 紧急恢复；
- 1.6 → 1.7 升级和应用回退保护。

文档命令必须在 CI 或演练中执行；告警必须引用上表真实指标，不添加不存在的指标名。

### 11.5 M4 完成条件

- health 故障注入测试通过；
- 指标名、类型和 label 基数测试通过；
- 支持包 canary secret、大小和超时测试通过；
- runbook 命令完成一次受控演练；
- `V17-OPS.PR` 可 PASSED，RC 演练后 `V17-OPS.RC` 才能 PASSED。

固定验收命令：

```bash
mvn -B -ntp -pl kairo-platform-server,kairo-cli,kairo-ops -am \
  test -Dtest='*Health*Test,*Metrics*Test,*SupportBundle*Test' \
  -Dsurefire.failIfNoSpecifiedTests=false
mvn -B -ntp -pl kairo-platform-server,kairo-cli,kairo-ops -am test
./scripts/v1.7/verify-runbooks.sh
git diff --check
```

`verify-runbooks.sh` 只执行文档中标记为可自动化的命令和链接检查；真实停库、恢复和紧急操作演练由
`target/v1.7/ops-result.json` 记录，不能用关键字扫描伪造通过。

## 12. M5：版本统一与正式发布物

### 12.1 版本和 build identity

- Maven reactor 统一为 `1.7.0`；子模块不得继续硬编码 `0.1.0-SNAPSHOT`。
- Platform Web package/image 统一为 `1.7.0`。
- Docker Compose 默认镜像 tag 统一为 `1.7.0`。
- `/actuator/info`、Agent status/registration、CLI/MCP `--version`、支持包和 release manifest 使用同一 build identity。
- 开发构建允许 `1.7.0-SNAPSHOT`，RC 使用 `1.7.0-rc.N`，最终 tag 对应 `1.7.0`；禁止最终 artifact 带 SNAPSHOT。

### 12.2 正式发布物清单

| 发布物 | 内容/用途 |
|---|---|
| `kairo-agent-bundle-1.7.0.tar.gz` | bootstrap API、agent bootstrap、agent core modern、attach CLI、ops CLI、LICENSE、启动示例 |
| `kairo-platform-server-1.7.0.jar` | 单 Platform API + scheduler |
| `kairo-cli-1.7.0.jar` | Platform 自动化 CLI |
| `kairo-mcp-1.7.0.jar` | MCP 服务 |
| `kairo-sdk-1.7.0.jar` | Java SDK |
| `kairo-platform-server:1.7.0` | Platform 容器镜像 |
| `kairo-platform-web:1.7.0` | Web 容器镜像 |
| `kairo-compose-1.7.0.tar.gz` | Compose、配置模板、升级说明，不包含开发 Token |

`kairo-demo`、测试 fixture、未 shaded 的内部 Agent 模块和 `kairo-sidecar` demo 流程不作为正式独立产品发布物。

### 12.3 供应链固定选择

为防止实现者同时建设多套系统，V1.7 固定为：

- SBOM：CycloneDX；Maven 和 Web 分别生成，最终 release manifest 聚合；
- hash：SHA-256，生成 `SHA256SUMS`；
- 漏洞扫描：对最终 CycloneDX SBOM 执行一套 CI 扫描器，Critical/High 阻断，例外使用仓库内有到期日的 JSON；
- 许可证：allow/deny/exception JSON，AGPL 仓库自身和第三方依赖分别记录；
- 签名：Sigstore/cosign keyless，仅在受控 release CI 使用；本地没有 OIDC 可以 SKIPPED，但 RELEASE 不可 SKIPPED；
- provenance：CI 生成，subject hash 必须等于 release manifest 中 artifact hash；
- 不建设自有签名服务、不接入云 KMS、不同时维护 GPG 第二套发布链路。

### 12.4 可复现构建

- 从两个干净 checkout、相同锁定工具链构建；
- 归档 entry 顺序、时间戳、文件权限和 build metadata 必须确定；
- 两次 artifact hash 必须一致；
- 若镜像因 OCI metadata 无法直接字节一致，应比较规范化 image manifest/layer digest，并记录唯一允许的非确定字段；
- 不能通过忽略所有 metadata 或只比较文件名宣称可复现。

### 12.5 release manifest 最低字段

```text
schemaVersion
version
gitCommit
sourceTag
contractBaseline
buildWorkflow
buildStartedAt/buildEndedAt
toolchain
artifacts[]: name, type, sha256, size, sbom, signature, provenance
compatibilityEvidence
recoveryEvidence
performanceEvidence
soakEvidence
knownLimitations
ltsStart/standardSupportEnd/securitySupportEnd/maintainer
approval
```

### 12.6 M5 完成条件

- 最终清单中的 artifact 全部能从干净 checkout 生成；
- 两次隔离构建比较通过；
- SBOM schema、漏洞、许可证、hash 和本地 provenance 校验通过；
- 签名在 RC 可演练，最终签名留到 M6；
- `V17-SUPPLY.PR` 可 PASSED，但 RELEASE 保持 NOT_RUN；
- 达到 `Engineering Complete`，尚不能创建 `V1.7.0` tag。

固定构建和验证接口：

```bash
./scripts/v1.7/build-release.sh \
  --version 1.7.0-rc.1 --output target/v1.7/release-a

./scripts/v1.7/build-release.sh \
  --version 1.7.0-rc.1 --output target/v1.7/release-b

./scripts/v1.7/verify-reproducible.sh \
  target/v1.7/release-a target/v1.7/release-b

./scripts/v1.7/verify-release.sh \
  --manifest target/v1.7/release-a/release-manifest.json \
  --require-signature false
```

`build-release.sh` 只能构建 §12.2 清单，不把 demo、测试 fixture、node_modules、源码工作区或本地密钥打入发布包。
M5 本地/PR 验证允许 `--require-signature false`；M6 最终验证必须为 true。

## 13. M6：LTS 候选与最终认证

M6 是发布活动，不是继续扩展产品的开发里程碑。

### 13.1 进入条件

- M0～M5 的 PR 门禁全部 PASSED；
- M1 恢复和 M3 兼容的 RC 门禁已在候选 commit 上 PASSED；
- 没有 P0/P1；P2 有 owner、规避方式和目标维护版本；
- release manifest 字段齐全，日期/责任方只允许在此阶段最终填写。

### 13.2 必须执行

1. 固定候选 commit，后续只接受阻断缺陷修复；修复后受影响 RC 门禁重新执行。
2. Linux/JDK 21/PostgreSQL 16/Redis 7 执行 1.6 → 1.7 升级、备份恢复和应用回退保护演练。
3. 执行 M3 最终兼容矩阵，结果必须追溯到候选 commit。
4. 执行 10,000 cycle、2 小时 RC soak 和最终固定 7 天 soak。
5. 从干净 checkout 构建最终 artifact，生成最终 SBOM、扫描、hash、cosign 签名和 provenance。
6. 独立验收者按 manifest 复现所有适用门禁。
7. 生成最终支持矩阵、已知限制、升级/备份/恢复说明和 LTS 日期。

固定发布接口：

```bash
./scripts/v1.7/run-upgrade-rehearsal.sh \
  --from V1.6.0 --to HEAD --database postgresql16 \
  --output target/v1.7/upgrade

./scripts/v1.7/run-state-cycle.sh \
  --cycles 10000 --output target/v1.7

./scripts/v1.7/run-soak.sh \
  --duration P7D --output target/v1.7

./scripts/v1.7/build-release.sh \
  --version 1.7.0 --output target/v1.7/final

./scripts/v1.7/verify-release.sh \
  --manifest target/v1.7/final/release-manifest.json \
  --require-signature true
```

兼容矩阵由权威 GitHub workflow 执行；最终 manifest 只能引用该候选 commit 的 workflow run 和 artifact，
不能引用开发分支早期成功结果。

### 13.3 发布阻断

以下任一项阻断发布：

- 任一承诺矩阵行 FAILED/SKIPPED/NOT_RUN；
- 7 天 soak 未完成、异常退出或资源/状态超过预算；
- P0/P1 未清零；
- Critical/High 漏洞无有效例外；
- 最终 artifact 缺 hash、SBOM、签名或 provenance；
- 文档支持声明与兼容 JSON 不一致；
- manifest 缺维护责任方或支持日期；
- 独立验收无法从干净 checkout 复现。

### 13.4 完成动作

只有全部 RELEASE 门禁 PASSED 后才允许：

1. 合并最后阻断缺陷修复；
2. 发布最终 artifact；
3. 创建 annotated tag `V1.7.0`；
4. 将状态从 `Engineering Complete` 改为 `LTS Certified`；
5. 宣布 V1.X 功能开发结束并进入 V1.7.x 维护。

## 14. 通用 PR 门禁

每个产品代码工作包至少执行：

```bash
mvn -B -ntp test

cd kairo-platform-web
npm run lint
npm run typecheck
npm run test
npm run build

cd ..
git diff --check
```

如果只修改与 Web 无关的底层模块，开发智能体可以先运行聚焦 Maven 测试，但交付给主验收前必须执行完整 reactor；
Web 全门禁可以由主验收在里程碑末统一执行，不能在最终证据中省略。

任何测试失败都必须保留失败原因；禁止：

- 删除失败测试；
- 放宽有价值断言；
- 增大 sleep/timeout 掩盖竞态；
- 使用 `continue-on-error` 让承诺门禁变绿；
- 把真实 Agent/JVM 场景替换成只操作内存 Map 的模拟测试。

## 15. Claude Code 执行协议

本节是给开发智能体的强制执行边界。

### 15.1 一次只做一个工作包

任务必须明确写出 `M1-A`、`M2-B` 等唯一编号。Claude 不得在一个会话中“顺便完成后续里程碑”。完成当前
工作包的验收后立即停止并交付。

### 15.2 开工步骤

1. 读取本文、当前 `git status`、`git diff` 和该工作包允许修改的文件。
2. 保留所有现有未提交工作；不得 reset、checkout 或覆盖其他智能体修改。
3. 列出当前代码与工作包验收条目的映射。
4. 只实现缺失条目；已有正确实现不重写。
5. 若必须修改允许范围外的产品模块，停止编码并提交设计偏差说明。

### 15.3 实现约束

- 不新增同义状态、同义 DTO、第二套 registry 或旁路数据库表。
- 不引入本文未批准的基础设施、中间件、框架或运行时依赖。
- 不改变 V1.6 业务语义和 M0 基线；协议只能 additive。
- 不为了“代码优雅”进行大范围重构。
- 优先复用现有 `Clock`、revision/hash、idempotency、fencing、audit 和 PlatformException。
- 遇到不确定状态时 fail closed 到可诊断状态，不假报成功。
- 不 commit，由 Codex 审查、验收和提交。

### 15.4 交付格式

Claude 完成后必须输出：

```text
工作包：<ID>
已完成：逐条对应本文编号
未完成：逐条列出，不得省略
修改文件：每个文件及目的
数据/协议/API变化：含兼容说明
测试命令：原样列出
测试结果：通过数、失败数、报告路径
真实 JVM证据：场景和结果
风险：仍可能失败的边界
建议主验收步骤：精确命令
```

### 15.5 强制停止条件

出现以下任一情况，Claude 必须停止扩大实现并报告：

- 需要改变本文可靠性语义；
- 需要修改 V1～V41 迁移；
- 需要增加新产品 API/Web 页面；
- 需要支持 JDK 8/11、Windows 或多 Platform 节点；
- 需要持久化脚本正文、原始 class bytes 或 Token 才能“恢复”；
- 当前失败来自前一工作包未完成；
- 聚焦测试连续失败且根因不在当前允许范围；
- 只能通过降低测试质量才能继续。

主验收者决定修改方案、建立 bugfix 分支或退回前一工作包。开发智能体不得自行扩大范围。

## 16. 缺陷分支与集成规则

- 正常工作在 `V1.7`；发现独立 V1.6/V1.7 缺陷时使用 `bugfix/v1.7-<scope>`。
- bugfix 必须包含修复前失败、修复后通过的测试。
- bugfix 合并回当前最近的 V1.7 分支后，再继续后续工作包。
- 不机械 cherry-pick 大提交；共享 mapper/协议冲突由主验收者统一处理。
- 每个工作包经 Codex 审查、独立测试并提交后，才能作为下一个工作包基线。

## 17. Requirement 与里程碑映射

| Requirement | 负责里程碑 | PR 证据 | RC/RELEASE 证据 |
|---|---|---|---|
| `V17-CONTRACT` | M0 | freeze tests | 最终 commit 重跑 |
| `V17-RECOVERY` | M1 | M1-A～F 集成/真实 JVM | 生产拓扑故障演练 |
| `V17-UPGRADE` | M0/M1-F/M6 | 历史 fixture | PostgreSQL 16 备份恢复演练 |
| `V17-PERF` | M2 | 短程基准 | 固定 runner 比较 |
| `V17-SOAK` | M2/M6 | 短程循环 | 2 小时 RC、7 天 RELEASE |
| `V17-COMPAT` | M3 | fixture/schema | 最终候选真实矩阵 |
| `V17-OPS` | M4 | health/metrics/脱敏测试 | runbook 演练 |
| `V17-SUPPLY` | M5/M6 | 构建/hash/SBOM schema | 最终扫描/签名/provenance |
| `V17-LTS` | M6 | 策略文档 | 日期、责任方、批准 |
| `V17-REGRESSION` | 所有 | Maven/Web 全回归 | 最终干净 checkout |

## 18. 最终完成定义

### Engineering Complete

满足以下条件时，可以说“V1.7 代码开发完成”：

- M0～M5 全部完成；
- 所有 PR 门禁 PASSED；
- M1 恢复闭环存在真实 JVM 证据；
- 发布候选物可从干净 checkout 生成；
- 没有已知 P0/P1。

此时不能说“V1.7.0 LTS 已发布”，也不能创建最终 tag。

### LTS Certified

只有 M6 的兼容、升级、2 小时 RC、7 天 soak、最终供应链和独立验收全部通过，且 release manifest 已填写
支持日期、责任方和批准人，才算 V1.7.0 真正完成。

如果外部环境、签名权限或时间尚未满足，状态保持 `Engineering Complete / RELEASE NOT_RUN`，不得用本地单测
替代，也不得继续增加功能填补等待时间。
